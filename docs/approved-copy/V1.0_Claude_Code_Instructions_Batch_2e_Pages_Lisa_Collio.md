# Claude Code Instructions — Batch 2e, Part 1: Spanish Goshen Pages

*Lisa Collio website · lisacolliorealtor/lisa-collio-website · July 2026*
*Governing documents: Master Plan v2.8 (§6, §7, §8), Build Plan v1.2 (Batch 2e), CLAUDE.md. One-time task file — safe to delete after this PR merges.*

**PRECONDITION: MET.** Both page files are **APPROVED BY LISA and signed off by the managing broker — 24 July 2026 (Master Plan §13 gate cleared)**. Cleared for build. Lisa's merge is the only publish trigger.

**Source files (this PR):**
- `V1.0 ES Mudarse a Goshen Page Lisa Collio.md` → new page `/es/mudarse-a-goshen/`
- `V1.0 ES Viviendo en Goshen Page Lisa Collio.md` → replaces `/es/comunidades/goshen/` at new path `/es/viviendo-en-goshen/`

Publish ONLY the content between each file's `═══ PAGE CONTENT ═══` markers. Strip all internal blocks (SEO/AEO Notes, Featured Image Specs, Fix Log, Adaptation Notes). Use the notes to build title tags, meta descriptions, schema, images, and links.

---

## 0. Store the source documents in the repo (FIRST STEP — standing rule)

Lisa will attach the three approved files to this Claude Code session. Before building anything, commit all three into the repo under `docs/approved-copy/` (create the folder if it doesn't exist), keeping their exact filenames. **Standing rule from Lisa (24 July 2026): Claude Code always stores the source documents it works from in the repo itself — Lisa never uploads files to GitHub manually.** Apply this in every future batch as well.

---

## 1. New page: /es/mudarse-a-goshen/

1. Build from the mudarse file. `lang="es"`. Title tag, meta description, canonical per its SEO notes.
2. Schema: WebPage + Article + FAQPage (visible FAQs) + site-wide RealEstateAgent. FAQPage schema in Spanish, matching on-page text exactly.
3. hreflang pair with `/moving-to-goshen/`: `en`, `es`, `x-default` → EN. Add the pair declaration to BOTH pages.
4. Language toggle on this page: "🌐 English →" targets `/moving-to-goshen/`.
5. Featured image: Template A, 1200×630, overlay text exactly (accents included): **Mudarse a Goshen, Indiana: Lo que debe saber antes de dar el paso**. RE/MAX Blue #003DA5 or Red #E60E16, white text — consult and update the four-variant rotation log. No separate og:image needed for Template A.
6. Apply the locked Spanish footer partial. Do not retype footer content into body copy.
7. Breadcrumb: Inicio · Mudarse a Goshen.
8. The six FAQ article links point to locked future slugs under `/blog/spanish/` (listed in the file's Internal Link Map). They will 404 in previews until the Batch 2e Part 2 articles PR merges — this is expected and accepted pre-cutover. Do NOT invent stub pages; both PRs merge before DNS cutover.

## 2. Rename + rewrite: /es/comunidades/goshen/ → /es/viviendo-en-goshen/

1. Build the new page at `/es/viviendo-en-goshen/` from the viviendo file. Same schema pattern plus **Place**. `lang="es"`.
2. **301 redirect in this same PR** (Master Plan §6 rule, no exceptions): `/es/comunidades/goshen/` → `/es/viviendo-en-goshen/` via `_redirects` or `netlify.toml`, matching the pattern used for the English renames in Batch 2f.
3. Retire the old page file entirely — the rewrite supersedes it in full.
4. hreflang pair with `/living-in-goshen/` on BOTH pages. Language toggle targets `/living-in-goshen/`.
5. Featured image: Template A, 1200×630, base `assets/images/goshen/downtown-main-street.jpg`, overlay text exactly: **Vivir en Goshen, Indiana: Guía local de la ciudad**. Check the rotation log.
6. Breadcrumb: Inicio · Vivir en Goshen (drop the old "Comunidades · Goshen" pattern).
7. The five FAQ links point to the five EXISTING live Spanish articles — slugs unchanged, verify against the built blog, don't guess.

## 3. Site-wide link + nav updates (same PR)

1. **Grep the full repo for `/es/comunidades/goshen/`** and update every internal link, canonical, sitemap entry, and schema reference to `/es/viviendo-en-goshen/`. Do not rely on any list being exhaustive.
2. **EN /living-in-goshen/:** the "Hablo español" banner currently targets `/es/comunidades/goshen/` — update to `/es/viviendo-en-goshen/`. Also fix its breadcrumb, which still reads "Home · Communities · Goshen" — change to "Home · Living in Goshen" to match the Batch 2f nav labels.
3. **EN /moving-to-goshen/:** the "Hablo español" banner currently targets `/es/` — update to `/es/mudarse-a-goshen/`.
4. **Spanish nav — mirror the Batch 2f two-level structure** (Master Plan §6, Site Navigation subsection):
   ```
   Comunidades ▾
     Goshen                        ← group header, not a link
       Mudarse a Goshen  → /es/mudarse-a-goshen/
       Vivir en Goshen   → /es/viviendo-en-goshen/
     Elkhart             → single flat link → /es/comunidades/elkhart/
   ```
   Elkhart stays a flat link at its CURRENT path — do not rename `/es/comunidades/elkhart/` in this PR; that happens with its rebuild (Batch 4). Do not build Elkhart as a one-item dropdown. Mobile pattern: same accordion/expand approach chosen for the EN nav in Batch 2f.
5. Update `sitemap.xml`: add `/es/mudarse-a-goshen/`, replace the old ES Goshen path with `/es/viviendo-en-goshen/`.

## 4. Reviews (Batch 2b rotation)

Both new pages participate in the standard rotation: 1 review with matched photo + 1 review without photo + 1 standalone client-general photo. **Spanish-language reviews only on these pages** (verbatim originals, or labeled translations of English reviews per the Batch 2b rule). No repeats on the same or adjacent pages. Attribution: first name + last initial unless full-name consent is documented.

**RESOLVED by Lisa, 24 July 2026:** `/moving-to-goshen/` keeps "Maria Baltazar" as displayed — the attribution matches the name exactly as it appears publicly on the Google review, for consistency with the source. No change needed. (Scope of this rule beyond this one review — see Lisa's pending confirmation in the project record; until confirmed, all other reviews stay first name + last initial.)

## 5. Footer dependency note

The Spanish footer partial currently lacks the general-content disclaimer line (Master Plan §8, footer stack item 2.5) and the Disclaimers/Sources links — that gap is **Batch 2c scope** (Spanish legal pages + ES footer fixes), which precedes DNS cutover. If Batch 2c has merged by the time this PR builds, these pages inherit the corrected footer automatically; if not, do not patch the footer here — keep it in 2c so it lands once, site-wide.

## 6. Verification checklist before requesting Lisa's review

- [ ] Both pages render at their new URLs in the deploy preview
- [ ] 301 works: `/es/comunidades/goshen/` → `/es/viviendo-en-goshen/`
- [ ] Repo grep returns zero remaining references to `/es/comunidades/goshen/` (outside the redirect rule)
- [ ] hreflang pairs validate on all four pages (2 EN + 2 ES)
- [ ] Both "Hablo español" banners on the EN pages target the correct ES pages
- [ ] ES nav shows the two-level Goshen group; Elkhart remains a flat link
- [ ] Language purity: no English strings in ES page bodies except the toggle
- [ ] Featured images match spec, overlay text exact with accents, rotation log updated
- [ ] FAQPage schema matches visible FAQ text
- [ ] Footer is the locked Spanish partial, not retyped
- [ ] No page displays price figures — all data routes to /es/estadisticas-del-mercado/
