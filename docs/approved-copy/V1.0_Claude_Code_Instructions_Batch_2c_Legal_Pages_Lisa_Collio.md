# Claude Code Instructions — Batch 2c: Spanish Legal Pages + ES Footer + Consent Checkboxes

*Lisa Collio website · lisacolliorealtor/lisa-collio-website · July 2026*
*Governing documents: Master Plan v2.8 (§6, §7, §13, §18), CLAUDE.md. One-time task file — safe to delete after this PR merges.*

**WORKFLOW STATUS: Source file is APPROVED BY LISA and signed off by the managing broker — 24 July 2026. §13 gate cleared for merge.** Batch was built on a draft branch per the standing workflow; the source file has been moved from `docs/drafts/` to `docs/approved-copy/` with the stamp. Cleared to merge.

**DEPENDENCY:** Batch 2c must merge before Batch 2b (review rotation) and before DNS cutover. Confirm Batch 2e (both PRs — pages and articles) has merged first; if not, this batch can still proceed independently since legal pages don't depend on the Goshen cluster.

---

## 0. Store the source documents in the repo (FIRST STEP — standing rule)

Commit the attached files into `docs/drafts/` (draft) or `docs/approved-copy/` (if already stamped approved), keeping exact filenames. Standing rule: Claude Code always stores its own source documents in the repo — Lisa never uploads to GitHub manually.

---

## 1. Build the four Spanish legal pages

Source: `V1.0 ES Legal Pages Lisa Collio.md`. Publish ONLY the content between each page's `═══ PAGE CONTENT ═══` markers — this includes the courtesy-version/English-controls paragraph at the end of each page; that paragraph is part of the published page, not internal notes. Strip the file intro, the "SEO (internal)" blocks, and the end Notes.

| Page | URL | hreflang pair |
|---|---|---|
| Términos de Uso | `/es/terminos/` | `/terms/` |
| Aviso de Privacidad | `/es/privacidad/` | `/privacy/` |
| Accesibilidad | `/es/accesibilidad/` | `/accessibility/` |
| Vivienda Justa | `/es/vivienda-justa/` | `/fair-housing/` |

1. `lang="es"` on all four. Declare hreflang pairs (`en`/`es`/`x-default`→EN) in both directions — update the English pages too if they don't yet declare the pair.
2. **Effective dates:** each Spanish page's effective date must match its English counterpart's effective date exactly — same date, not the Spanish page's own publish date. Check what date is set (or will be set) on the English pages first, then apply it to both language versions together.
3. Apply the locked Spanish footer partial to all four (see Section 2 below — the footer itself needs fixing first).
4. Fair Housing page: display the Equal Housing Opportunity logo on `/es/vivienda-justa/` itself, matching the English page's placement, in addition to the site-wide footer instance.
5. Title tags per the SEO notes block in the source file.

## 2. Fix the Spanish footer partial

The ES footer partial currently lacks two things the EN footer has (flagged in Batch 2e as a known gap — fix now):

1. **The general-content composite disclaimer line** (Master Plan §8, footer stack). Translate/mirror the EN disclaimer's exact legal meaning — no adaptation, same precision standard as the legal pages themselves. Insert it at its locked position in the footer stack: signature line → composite disclaimer → agent-not-lender statement (both languages) → Equal Housing Opportunity → RE/MAX independence line → legal page links.
2. **Legal page links** — the footer must link to all four Spanish legal pages (`/es/terminos/`, `/es/privacidad/`, `/es/accesibilidad/`, `/es/vivienda-justa/`), not to the English pages and not omitted.

Verify the footer stack order matches the locked order above exactly, on every Spanish page site-wide (not just the four legal pages) — this is a shared partial, so one fix should propagate everywhere, but confirm it actually does by checking at least three different ES page types (a main page, a community page, an article).

## 3. Fix Spanish form consent-checkbox links

Find every Spanish-language form on the site (lead-capture, home-valuation, contact, any others). Each has a consent checkbox that links to the Privacy Policy and/or Terms — audit and fix:

1. Any Spanish form whose consent checkbox currently links to the **English** legal pages must be updated to link to the Spanish pages (`/es/privacidad/`, `/es/terminos/`).
2. Checkbox label text itself must be in Spanish (grep for any leftover English consent copy on ES-page forms).
3. Test each Spanish form end-to-end after the fix: checkbox renders, links resolve to the correct ES legal page, form still submits successfully to Netlify Forms.

## 4. Sitemap + site-wide link check

- Add all four new ES legal URLs to `sitemap.xml`.
- Grep the repo for any existing internal links pointing to English legal pages FROM Spanish page content — these should point to the Spanish versions instead (except the courtesy-clause text itself, which correctly names "la versión en inglés" as prose, not a link requirement).

## 5. Verification checklist before requesting Lisa's review

- [ ] All four Spanish legal pages render at their URLs in the deploy preview
- [ ] Effective dates match their English counterparts exactly (same date, both languages)
- [ ] hreflang pairs validate in both directions on all four pairs
- [ ] Courtesy-version/English-controls paragraph appears at the bottom of all four Spanish pages
- [ ] ES footer now includes the composite disclaimer line and links to all four Spanish legal pages, in the correct locked stack order
- [ ] Footer fix confirmed on at least 3 different ES page types, not just the legal pages
- [ ] Every Spanish form's consent checkbox links to the Spanish legal pages, with Spanish label text
- [ ] All Spanish forms tested end-to-end post-fix (checkbox + submission)
- [ ] sitemap.xml updated with all four ES legal URLs
- [ ] Equal Housing Opportunity logo displays on `/es/vivienda-justa/` and site-wide ES footer
- [ ] No English legal-page links remain in Spanish page content outside the courtesy-clause prose
