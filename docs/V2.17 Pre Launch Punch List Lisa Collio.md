# Pre-Launch Punch List — v2.17

*Lisa Collio, Real Estate Agent · 29 July 2026*
*v2.17 (17 August) delivers five Home page body images (two of Lisa's own photographs, three AI-generated illustrations — the site's first) and the policy change they required: Master Plan §8's Images section amended (v2.22) to permit AI-generated illustrations as broker-approved body images under stated conditions, Hero Standard bumped (v1.1) with a clarifying sentence keeping heroes excluded, CLAUDE.md gains the rule it never mirrored (v2.6). Fixes a real-world upload-location problem (GitHub's create-file flow produced a stray blob instead of a directory) rather than treating it as expected. Logs `real-estate-guidance.jpg`'s under-floor resolution (957×958) as a post-launch replacement candidate. Also corrects the Hero Standard's own five-day-stale "DRAFT, not built" status line, found while editing the same document for an unrelated reason. **Same PR, two changes requested on Lisa's review before merge:** `lisa-collio-knows.jpg` cropped tighter to its subject (3024×4032 → 2945×2120, boundaries proposed and approved before applying); Home's `faq-item--media` EN/ES asymmetry (4 images EN, 0 ES) fixed rather than deferred, on Lisa's explicit ruling — the same four files added to the Spanish FAQ with newly drafted, Lisa-approved Spanish alt text, and the image-parity baseline re-measured from scratch to true 20/20 (the pair then removed from the baseline file entirely, since `audit.js` only tracks gaps, not matches). Full detail in the new §A item.*
*v2.16 (17 August) unifies review-card quote marks sitewide — a Phase 0 recon widened Lisa's report of one broken card into a field-class fix across three previously-inconsistent behaviors and 8 hand-authored pages the original report hadn't covered — and replaces the review block's static "Lisa's clients" caption with a rotating thank-you message in Lisa's own voice (`content/thank-you-messages.json`, 15 EN + 15 ES, independently authored). New standing rule: the thank-you card must never carry a Master Plan §17 "Translated from" label. Closes the 109-stale-rotation-pages item as a side effect. Full detail in the rewritten §E item.*
*v2.15 (17 August) closes the content-hashed-asset-filenames item — the last technical blocker before DNS cutover tracked under §G — via query-string content hashing (Option B) rather than renaming files on disk (Option A), chosen after a Report-Before-Build Phase 0 recon measured the actual reference surface (2,734 raw occurrences, 499 distinct paths, five reference patterns) and checked both options against the real `audit.js` regexes rather than reasoning about them in the abstract. New `hash-assets.js` rewrites all five patterns (plus a sixth, closely-related one found during this build's own verification); `build-reviews.js` gained its own hash computation via new `hash-util.js` so its independently-generated markup never regresses; new `audit.js` check 30 closes the srcset/og:image/twitter:image/JSON-LD-image verification gap the recon identified, proven both ways. Netlify headers for CSS/images/favicons are now `immutable`, one year. Full detail in the rewritten §G item.*
*v2.14 (17 August) closes the deferred Tier 2 hero-height item and delivers the approved focal-point mechanism build: `--hero-focal-x`/`--hero-focal-y` custom properties (both axes, defaulting to 50% so no page changes unless it opts in), the axis-flip finding recorded in both the Hero Standard and here, all fourteen approved focal-point numbers across seven pages (EN+ES), three same-filename photo replacements (`moving-to-goshen.jpg`, `living-in-goshen.jpg`, `moving-to-elkhart.jpg`, all MPO-container fixes handled by the existing pipeline), and the Tier 2 height fix (551px mobile / 488px desktop, up from 423/376, measured with Playwright). Full detail in the rewritten §A item.*
*v2.13 (13 August) closes the twelve-hero-photographs item: all twelve real photos delivered and wired (24 pages, EN+ES), replacing the shared placeholder from v2.11. Closes the check-26 flip-to-blocking item (now ERROR, re-proven by mutation test). Records four MPO-format fixes, three post-launch-replacement candidates (under the 2400px floor, shipped anyway per Lisa's ruling), the new `content/source/licensed-assets.txt`, and Lisa's standing policy narrowing what gates an image (Fair Housing, rights to use, whether it's hers to publish — legible signage/dated content/crop composition are no longer findings on their own). Fixes a stale pointer to the pre-rename script filename left in the v2.12 entry it's closing alongside. Defers the Tier 2 hero's cramped mobile/desktop height to its own future PR, out of scope here.*
*v2.12 (13 August) closes the mobile-nav open-panel reachability bug (live since PR #71, ~2.5 weeks, unreachable Contact link on a phone) as its own tracked incident in §C: what was measured, why the originally-proposed fixed-pixel fix was rejected in favor of a self-adjusting one, the fix actually shipped, and the reachability proof at three widths in both languages. Adds `audit.js` check 29 to the mechanical-checks table, proven three ways (true positive against the real pre-fix file, true negative, and a mutation test whose own result surfaced that the fix's two escape hatches are independently redundant). Opens a separate, deliberately-unresolved §C item: no check in this project renders a page and measures it, which is why this bug was invisible to 27 existing checks and a full site audit — recorded as a gap for a later session to decide, not acted on here.*
*v2.11 (13 August) adds the Hero Standard v1.0 build's punch-list footprint: a §A item for the twelve pending hero photographs (Sellers flagged separately as third-party licensed, not Lisa's own photography — Hero Standard §5), a §C decision item to flip `audit.js` check 26 from warning to blocking before launch, and a §C closure of Home's hero build-record gap flagged in this build's own Phase 0 report. Adds checks 26–28 to the mechanical-checks table and removes "hero copy scanned for banned terms" from the "Proposed, not built" row now that check 27 exists. **In-place correction, same day, before this version shipped to `main`:** Lisa asked directly what check 28 actually catches; the honest answer, verified against the true worst-case in-zone scrim opacity rather than assumed, is "nothing that can currently occur" for the contrast-threshold branch — it cannot fail for any photograph under the current CSS, and only the check's two infrastructure catches (broken image reference, missing sampler dependency) are real. The check-28 table row and the §A cross-reference are rewritten to say so plainly, and a new §C item opens the redesign decision rather than leaving a check in the table that reads as coverage it doesn't provide.*
*Supersedes `V1_0_Pre_Launch_Punch_List_Remaining_Lisa_Collio.md`. Consolidates that document with everything opened during the featured-image standard, Goshen section-image, EN/ES parity, and Fair Housing sweep work.*
*v2.1 added the meta/og description sweep, the buyers-cluster title-suffix inconsistency, and the full current state of mechanical checks including `OVERLAY_JOBS`.*
*v2.2 (30 July) records the description band-and-phone pass, corrects the "41 out-of-band descriptions" figure to 32, and opens the over-length-titles item that the other 9 warnings turned out to be.*
*v2.3 (30 July) closes the titles item and the buyers-cluster suffix item. **The audit now reports zero SEO warnings, down from 41.***
*v2.4 (30 July) brings the file current through PR #93. Adds a §B entry for each of PRs #88–93. Closes out the RV/Spanish featured-image title-drift item in §C with a verified — not assumed — outcome: the RV-industry article's image was never in scope for baked text at all, and the four Spanish `OVERLAY_JOBS` images are still unregenerated, unchanged since the decision was opened on 30 July. Adds a §G item recording that the Master Plan itself lived only outside the repo until PR #92 — the second time a governing document existed only outside git, the first being this punch list. Governing-documents line updated to Master Plan v2.14.*
*v2.5 (30 July) updates the governing-documents line to Master Plan v2.15, as part of the bundled version-bump change (new Master Plan version committed, v2.14 deleted, CLAUDE.md's citations updated) required by the Audit Checklist v1.3's new bundled-bump rule.*
*v2.6 (30 July) adds a §E item for the 53 pages with stale (not broken) review-rotation state, found while verifying content for the Audit Checklist v1.4 fold.*
*v2.7 (31 July) adds a §H clarification: five early-build ES documents' individual broker sign-off stamps are real, not a contradiction of the later comprehensive-review-at-the-end rule — corrected a false positive from the 30 July Part C2 repo audit.*
*v2.8 (31 July) makes the governing-documents line and the §I Full Site Audit reference version-agnostic (Master Plan and Audit Checklist, both previously hardcoded and both had already drifted at least once this session) — same fix applied across CLAUDE.md, the Audit Checklist's own header, and the four compliance references in `docs/compliance/`.*
*v2.9 (5 August) **reconciles the two parallel copies of v2.8 and retires the second one.** A punch list carrying PR #98's follow-ups had lived unmerged on `claude/compliance-gaps-copy-fields-scwmod` since 31 July while `main`'s copy moved on through PR #108 — two files, same name, same version number, each holding content the other never had. Three items folded in from that branch: the `/es/vendedores/` review-block gap (§D, OPEN), the sold-listing consent caveat (§E, CLOSED with Lisa's 2 August confirmation, recorded so the history shows the caveat existed rather than vanishing), and PR #98's §B entry. Two items dropped as superseded (an Elkhart-batch item overtaken by PRs #104–#107, and a Full Site Audit item pointing at a dead Audit Checklist filename that `main` already carries correctly). Branch retired. Adds the ES description-vs-CTA phone field-class ruling to §E.*
*v2.9 (cont.) records the **Master Plan v2.16 bundled bump**: v2.16 committed to `docs/approved-copy/`, v2.15 deleted in the same change, CLAUDE.md regenerated to v1.9 mirroring its six new standards, both amendments and the §19 scope ruling. Five corrections to v2.16's own account of the Elkhart batch are recorded in §C for a future v2.17 — reported, not applied, since the Master Plan is Lisa's to edit. The `FEATURED_IMAGE_OVERLAY_AUDIT.md` disagreement v2.16 left open is resolved in §C in favour of the audit, settled by `npm run check:images` rather than by argument.*
*v2.9 also **versions the in-place edits that had accumulated on top of v2.8 without a bump** — `audit.js` checks 22 and 23, the source-repoint rule, the 1:1 zoom-sheet standard, and the changelog-versions-are-data rule, all added 2 August at Lisa's direction and all sitting unversioned. And it **backfills the mechanical-checks table with every check missing from it** — 19, 20 and 21 (added by PR #98, recorded in no punch list at all) plus 22 and 23 (described in the body of this file but never added to the table). The table's stated purpose is telling future sessions what is already enforced; five enforced checks were invisible to it.*
*v2.10 (11 August) is a rename, not a content pass — the file stayed on the `V2.9` filename from 5 to 11 August while its content kept changing underneath it, because Master Plan §19 hardcoded that filename as a live pointer and renaming would have stranded it. Master Plan v2.20 fixed that (§19 now names the highest-numbered version, not a pinned one); this rename was the first opportunity to close the gap it left, and describes the full span since the v2.9 rename rather than only its most recent thread, so nothing that changed during those six days is present in the file but absent from every version's summary.*
*Full detail, this thread (PRs #114–#116): PR #114 closed Master Plan v2.19's row-1 self-citation and this file's own two matching "extinct by construction" over-claims. PR #115 fixed `build-reviews.js`'s `stars()` helper reading every English review's star rating aloud in Spanish (148 instances, 74 pages, `aria-label="5 de 5 estrellas"` with no `lang` parameter) via a scoped direct edit rather than a generator write run, added `audit.js` check 25 (screen-reader text must match page language, proven four ways), closed the false §D `/es/vendedores/` "review block is missing" claim with its root cause traced to commit `014f102`, corrected the stale rotation-backlog count from a never-re-measured 53 to a re-measured 109, and logged two new EN/ES parity items (the 13-vs-11 sold-listings-showcase gap; the "Artículos relacionados" `.visually-hidden` heading present on 27 Spanish pages and zero English ones). PR #116 completed the §19 sweep v2.19 left half-done (Master Plan rows 2–3, `audit.js`'s own header comment, and `scripts/generate-featured-images.py`'s already-broken pointer), widened `audit.js` check 24 to the whole governing-documents column (proven against a real known-bad case, not planted), deleted all six one-time Claude Code instruction files whose batch had merged — not just the one first flagged — with a recorded caveat on the one file whose merge status couldn't be re-confirmed by its usual check (missing `PIL`), and closed two long-unchecked §F "verify this landed" boxes as a side effect of that deletion's verification.*
*Summarized, 5–11 August (intervening sessions, detail lives in each entry's own dated sub-note — not duplicated here): the two parallel v2.8 copies were reconciled (§B, §D, §E items folded in, one branch retired); Master Plan v2.16 shipped and was then audited against the repo, producing v2.17 (eight corrections, three initially-reported findings withdrawn after Lisa supplied missing evidence — see §C) and the Repo-Scoped Review standing rule (a repo search proves absence-from-repo, never absence-from-reality); the five approved Goshen source documents were committed and diffed against their live pages (439 sentences compared, 8 divergent, all repaired); Master Plan v2.18 wrote the Review Language Standard into §17; and the Master Plan's own self-reference bug was tracked as a recurring, named property of that document (§C's STANDING NOTE) rather than re-discovered each time.*

> **Committed to the repo 29 July 2026 (Claude Code).** This file was still a
> chat upload when Lisa referred to it as living at `docs/`. That is the exact
> failure the file's own opening paragraph warns about, so it is now in the
> repo where it belongs. **Update it in place; do not start a parallel record.**
>
> **Update log — 29 July 2026, later session.** Items closed: the alt-text
> banned-terms sweep, the named-organization standard, the meta/og description
> sweep, the description audit-check decision, and the three unidentified
> buyers titles. Items added: §E og/twitter fields absent sitewide, §E phone-
> number convention in descriptions, §E over-length descriptions. §B cleared —
> all in-flight PRs merged. Checks table gained check 18. Lisa may want to bump
> this to v2.2; the filename was left alone to avoid two versions in flight.

**Why this file exists:** items in this project have repeatedly been lost at session boundaries — the Goshen image manifest drove an entire build while existing only as a chat upload; PR #76's findings vanished when it was superseded; the rejected-asset ruling had to be re-established twice. **This file belongs in the repo** (`docs/`), not only in chat. Update it as items close.

**Governing documents:** the current Website Master Plan (always the highest-numbered version in `docs/approved-copy/`) · `docs/VERIFIED_FACTS.md` · `V1_0_Bilingual_FAQ_Authority_Article_Playbook_Lisa_Collio.md` · the current Audit Checklist (always the highest-numbered version in `docs/approved-copy/`)

---

## A0. Blog listing images — done except one open item (live compliance violation, needs Lisa's decision)

- [x] **Blog listing images (Report-Before-Build) — DONE, 17 August 2026, except the open live-violation item below.** `/blog/` and `/blog/spanish/` were text-only; article thumbnails already existed (`{slug}-thumb.jpg`, built by the featured-image pipeline) but were never wired into either listing page.
  **Deleted: `assets/images/lisa/lisa-collio-for-sale-sign-summer.jpg` (+ `.webp`).** The banned number 574-975-0141 was baked into the yard-sign photo, legible. Standing rule (CLAUDE.md): delete on sight. Referenced nowhere, so nothing broke. Recorded here per the Rejected-Asset Standard's spirit — an unexplained deletion is exactly what that standard exists to prevent, even for a loose asset with no manifest entry to mark inline.
  **Banned-number sweep, run on the whole class, not just the one flagged file.** Every filename in the repo containing "sign," "banner," "flag," "graphic," "courthouse," or "listing" was opened and visually checked (`homes-goshen`/`homes-elkhart` have none matching that pattern at all). Six more files carry the same banned number, baked into the same yard-sign template, legible in each:
    - `assets/images/lisa/lisa-collio-for-sale-sign-winter.jpg` — same sign/session as the deleted summer photo; the blue number bar falls just outside this particular crop, but it is the same physical sign confirmed elsewhere.
    - `assets/images/lisa/lisa-collio-sold-sign-winter.jpg`
    - `assets/images/lisa/lisa-collio-sold-sign-selfie-winter.jpg`
    - `assets/images/lisa/lisa-collio-courthouse-listing.jpg`
    - `assets/images/lisa/lisa-collio-open-house-flag-fall.jpg`
    - `assets/images/lisa/lisa-collio-open-house-flag-summer.jpg`
    **Ruling (Lisa, 17 August 2026): delete all six, after checking each for a live reference first — a live reference changes the job from "delete an unused file" to "fix a live page," and gets stopped and reported rather than deleted.** Five had no live reference anywhere in the repo (confirmed by grep before deleting) and are **DELETED**: `for-sale-sign-winter`, `sold-sign-winter`, `sold-sign-selfie-winter`, `courthouse-listing`, `open-house-flag-fall`. All seven deletions (the original summer photo plus these five) are recorded in `content/source/rejected-assets.txt` (Ruling 4) with the reason, so a future session does not re-upload the same photos.
    **The sixth, `lisa-collio-open-house-flag-summer.jpg`, is NOT deleted — it is a live compliance violation, found by checking, not assumed clean.** It is the job-table source (`scripts/generate-featured-images.py`, outName `what-is-it-like-to-work-with-lisa-collio`) for the featured image on `/blog/community/what-is-it-like-to-work-with-lisa-collio/`. The exact delivered crop (1200×630, `focal_y` 0.45) was reconstructed and checked at full resolution: **the banned number is legible in it, live on that page right now**, and has been since whenever that page's featured image was built — this predates the current task entirely. Deleting the source would break the page's featured image outright, so it wasn't deleted. **Open item, needs Lisa's decision:** re-crop to exclude the sign's bottom bar, or replace the source photo — either way the built `-header`/`-thumb`/`.jpg`/`.webp` files and the page's `og:image`/`twitter:image` all need regenerating afterward, not just the source swapped.
  Checked clean (no phone number at all): `just-sold-graphic.jpg`, `just-sold-marketing-graphic.jpg` (correct number only), `open-house-flag-cottage.jpg`, `open-house-flag-red-shirt.jpg`, `best-realtor-ever-sign.jpg`, `remax-banner-balloons.jpg`, `seniors-expo-booth.jpg` (correct number only), `executive-club-award-graphic.jpg`, and the `client-general/` prop-sign photos sampled (different sign type entirely).
  **Near-miss, caught before commit, not after.** The first EN Relocation-category pick was `assets/images/goshen/goshen-moving-location-commute.jpg` — clean-looking, unused elsewhere, and it was wired into `/blog/index.html`. Cross-checking every pick against `content/source/rejected-assets.txt` afterward (a step that should have run *before* wiring, not after) found it already listed there: "map infographic with baked-in text," rejected 29 July 2026, `audit.js` check 15 would have failed the build on it. Caught and replaced before anything was committed — with `assets/images/elkhart/elkhart-moving-location-commute.jpg` (the historic Elkhart train depot; clean, not rejected, thematically apt for "moving in from somewhere else"). The park/playground photo considered as a backup (`elkhart-moving-elkhart-or-goshen.jpg`) is also on the rejected list (identifiable minors, no release, familial-status Fair Housing concern) and was never wired. **Standing rule, not a one-time note: every image pick is checked against `content/source/rejected-assets.txt` at selection time, before wiring — never after.** `audit.js` check 15 is a backstop for what slips through, not the primary check; it only ever catches a rejected slug after a build has already failed once. The list only does its job if it's consulted when a file is chosen, the same moment alt text and dimensions get checked — not bolted on as a final pre-commit sweep. This same discipline is exactly what surfaced the live `open-house-flag-summer` violation above — checking a six-item list for live references before acting on it, not after.
  **EN Part 1 — five category-card images, wired into `/blog/index.html`'s "Browse by topic" grid** (full source photo, `.local-card__img` pattern — matches the existing path-card treatment elsewhere on the site, not the 800×420 `.blog-card__thumb` crop used for individual articles): Buyers → `homes-general/exterior-single-story-ranch-driveway.jpg`; Sellers → `homes-general/interior-dining-kitchen-glass-table-oak.jpg`; Community → `goshen/goshen-living-downtown.jpg`; Relocation → `elkhart/elkhart-moving-location-commute.jpg` (see near-miss above); Market Updates → `hero/market-stats.jpg` (the same aerial neighborhood photo already serving as the `/market-stats/` hero — deliberate reuse, since Market Updates has only one article and anything from `homes-general` would read as "the picture for that article").
  **EN Part 2 — article thumbnails wired for 51 of 57 EN articles** into `/blog/index.html`'s flat "All articles" list, following the exact `.blog-card__thumb` pattern already live on `/blog/buyers/`, `/blog/sellers/`, `/blog/relocation/` (picture + webp source + jpg img, first card in the list `loading="eager"`, all others `loading="lazy"`). Alt text pulled from each article's own `blog-featured` image, not written fresh. 6 EN articles have no featured image at all and were left text-only (list below).
  **ES Part 2 — article thumbnails wired for 36 of 55 ES articles** into `/blog/spanish/index.html`'s six topic sections (Comprar, Vender, Conozca a Lisa, Goshen, Elkhart, Tu Próximo Capítulo™), same pattern, first thumbnail *per section* eager (4 sections have at least one thumbnail; Conozca a Lisa and Tu Próximo Capítulo have none at all — see missing list). **Build note:** the first automated pass silently skipped 3 of 39 matching blocks (`industria-rv-comprar-casa-goshen-indiana`, `trabajar-en-la-industria-de-rv-y-comprar-casa-en-elkhart-indiana`, `errores-evitar-vender-casa-goshen`) — caught by diffing the wired-count against the inventory count rather than trusting the script's own "done" report, and fixed by hand. Re-verified: 36/36 wired, matches the inventory exactly.
  **ES Part 1 — approved by Lisa and wired, all six**, as a single clean photo under each section's `<h2>` (not a card grid — Lisa's explicit instruction: Spanish gets images on its own six existing sections, not a new grid matching English). Comprar → `homes-general/exterior-red-wood-deck-backyard.jpg`; Vender → `homes-general/interior-kitchen-oak-cabinets-stainless-fridge.jpg`; Conozca a Lisa → `lisa/lisa-collio-headshot-red-top.jpg`; Goshen → `goshen/goshen-living-downtown.jpg` (same file as EN Community, different page — deliberate cross-language identity, not reached-for variety); Elkhart → `elkhart/elkhart-living-downtown.jpg`; Tu Próximo Capítulo → `hero/next-chapter-method.jpg` (the EN/ES pillar page's own hero photo — chosen specifically because the first instinct for this slot, an AI-illustration graphic with "Descubrir" baked into it as text, was both baked-in text and tied to one method step rather than the method as a whole; rejected for both reasons before it was ever proposed to Lisa). All six checked against `rejected-assets.txt` and confirmed clear.
  **Page weight:** 87 existing thumbnail pairs total ≈5.24 MB WEBP / ≈7.06 MB JPG (avg. 62 KB webp / 83 KB jpg each). `loading="lazy"` on every card but the first per list means initial paint only pulls in the above-the-fold handful — not a real performance problem for a page that exists to be scrolled.
  **The 25 missing thumbnails are left text-only — Lisa's decision, generating them post-launch, not a blocker.** Full list, by language and section, for her to work from later:
  *EN (6):*
  - [community] `does-next-chapter-method-cost-extra` — "Does Using The Next Chapter Method™ Cost Anything Extra?"
  - [community] `how-long-does-next-chapter-method-take` — "How Long Does Each Step of The Next Chapter Method™ Take?"
  - [community] `next-chapter-method-different-from-typical-process` — "How Is The Next Chapter Method™ Different From a Typical Buying or Selling Process?"
  - [community] `not-sure-which-life-chapter` — "What If I'm Not Sure Which Life Chapter I'm In?"
  - [community] `what-is-the-next-chapter-method` — "What Is The Next Chapter Method™?"
  - [market-updates] `elkhart-county-mid-year-market-update-2026` — "Elkhart County Mid-Year Market Update (2026)"
  *ES (19):*
  - [Conozca a Lisa, all 8] `ayuda-adultos-mayores-reducir-hogar`, `como-es-trabajar-con-lisa-collio`, `cuantas-casas-ha-vendido-lisa-collio`, `por-que-clientes-eligen-lisa-collio`, `por-que-lisa-collio-agente-bienes-raices`, `proceso-completo-en-espanol-lisa-collio`, `que-es-sres-adultos-mayores`, `que-hace-diferente-lisa-collio`
  - [Tu Próximo Capítulo™, all 5] `cuanto-tiempo-toma-tu-proximo-capitulo`, `en-que-se-diferencia-tu-proximo-capitulo`, `no-estoy-seguro-capitulo-de-vida`, `que-es-tu-proximo-capitulo`, `tu-proximo-capitulo-costo-adicional`
  - [Goshen, 6 of 11] `buen-momento-comprar-casa-goshen`, `centro-de-goshen-indiana`, `distrito-escolar-goshen-indiana`, `industrias-empleadores-goshen-indiana`, `lugares-emblematicos-goshen-indiana`, `por-que-es-conocido-goshen-indiana`
  **Incidental finding, logged not investigated:** `assets/images/goshen/goshen-moving-buying-from-distance.jpg` is an antique-parlor interior (pump organ, roll-top desk) — its filename doesn't match its content at all. A filename/content mismatch like this is exactly how an image ends up wired to the wrong slot; flagged for whoever next touches that file, not chased down here.
  **Structural finding, recorded, decided by Lisa:** `/blog/spanish/` has no topic-card grid and no flat all-articles list — it's organized into six `<h2>` sections that don't map 1:1 onto the five EN blog categories (no Spanish Relocation or Market Updates section; those topics live inside the Goshen/Elkhart sections instead). Lisa's ruling: give images to the six sections as they actually exist rather than building a new grid to match English.

## A. Needs a photo from Lisa

Nothing here can be resolved by Claude Code. All are blocked on original photography.

- [ ] **Four rejected assets — replacements needed.** These four filenames are recorded in `content/source/rejected-assets.txt` and enforced by `audit.js` check 15. They are **not** oversights; they are confirmed not to be Lisa's original photography and must never be wired to a page:
  - `goshen-living-why-lisa.jpg` — stylised illustration
  - `goshen-moving-cta-closing.jpg` — stylised illustration
  - `goshen-moving-faq-moving-from-out-of-state.jpg` — composite graphic
  - `goshen-moving-location-commute.jpg` — map infographic with baked-in text (also breaches the no-text-on-images standard)
- [x] **Maple City emblem — CLOSED 17 August 2026, under the narrowed Image Review Policy, not by reshoot.** `goshen-living-faq-known-for.jpg`. The source is portrait, 3583×4048. A 1.9:1 band keeps only about **46% of the source height**, and the two halves of the wordmark sit at roughly **12–28%** and **72–88%** — at opposite ends of a tall circular emblem, currently running at `focal_y` 0.68 with "GOSHEN" trimmed at the top. **Nothing was reshot or re-cropped — this closes because the reason it was open no longer gates.** It was open purely for wordmark legibility/crop composition, with no Fair Housing, rights, or consent dimension; Lisa's 13 August 2026 policy change (recorded below) retired legible-signage/crop-composition judgments as blocking criteria unless they are a legal problem, and Master Plan v2.21 §8 now records that policy as governing. The leaves-only crop ships as-is. **This is a closed preference, not completed work:** Lisa may still reshoot the emblem in landscape post-launch if she wants the full wordmark to read, but nothing requires it.
  **A re-upload of the same file will not fix it, if a reshoot is ever wanted.** That was tried on 29 July: commit `9a588b3` was an *empty* commit — its tree matched its parent's and the file was still blob `0de49f09…`, byte-identical to the original already in `SECTION_JOBS`. The same portrait went in and the same fragmented crop came out. `main` never held a cropped version, so re-uploading the original changes nothing. A future reshoot would need the emblem **shot in landscape**, framed so the full circle fits a 1.9:1 band — or a different photo for that FAQ slot.
- [x] **Elkhart batch — PROCESSED 2 August 2026. 25 of 27 built; 1 rejected, 1 never in scope.** All 27 files pass filename, EXIF-provenance and full-decode integrity. 25 are processed into `assets/images/sections/` (100 files) and 24 article featured images rebuilt in `assets/images/blog-headers/` (96 files). `SECTION_JOBS` carries 25 new entries with source, focal point, crop and blob SHA; `CLEAN_JOBS` has 18 repointed and 6 added. `--sections` re-run reproduces all 100 files byte-for-byte. **Not wired to any page** — that is PRs 2–4.
  - **Reversals applied 2 Aug:** `elkhart-living-rivers-parks-outdoors` reinstated (Lisa's rejection crossed with her decision to keep it) — alt-text constraint stands, name no business in frame. `elkhart-moving-faq-older-home-checklist` hold released — Lisa holds written consent for the legible "916".
  - **Crops settled by Lisa:** clock tower `focal_y` 0.15, portrait 0.66, school 0.30, deer sculpture **0.28 (antlers intact, pedestal lost)**.
- [x] **`elkhart-moving-elkhart-or-goshen` — REJECTED, final (Lisa, 2 Aug 2026). The "Elkhart or Goshen?" section is image-free in both languages.** The replacement playground photo is Lisa's own and passes provenance and integrity with no legible signage — but it holds at least five children and one adult, several recognisable at the delivered crop. Rejected on two grounds alt text cannot cure: **identifiable minors with no release on file**, and **familial status** — a protected class — illustrating a where-should-I-live comparison. Lisa's framing: this is the same standard that permanently excludes all Jordi B photos and the Yaquelin Lozano pair, and strangers are a weaker position than family, not a stronger one. Recorded as **Ruling 3** in `content/source/rejected-assets.txt` and the manifest, explicitly in the same category as the church-signage rejection rather than the Goshen "not Lisa's photography" ruling. Precedent for the empty slot: "Where people work" on `/living-in-goshen/`; the title is also an abstract comparison that no literal photograph fits. A replacement, if ever wanted, must be a place with **no people and no legible signage**.
- [x] **LESSON — "not wired to any page" was true of the class and false in effect. Repointing `CLEAN_JOBS` changes what pages display without touching their HTML.** PR #104 repointed 18 `CLEAN_JOBS` entries and rebuilt the featured images **under their existing filenames**. No page HTML changed, so the PR reported "no image is wired to any page" — true of `section-figure` and `faq-item--media`, and false in effect: 18 article pages immediately began displaying an Elkhart photo while their alt text still described the house photo that used to be there. Screen-reader users were told about a backyard deck on a page showing a river. Live on `main` from the moment #104 merged until PR 2 fixed it.
  **Standing rule, for the Master Plan:** *any* source repoint must check whether the existing alt text still describes the new image. Filename-stable asset replacement is invisible to a diff of the HTML — the page changes without the page changing. The same trap applies to any future `CLEAN_JOBS`, `SECTION_JOBS` or `OVERLAY_JOBS` repoint, and to a re-upload under an existing filename.
  **Fixed in PR 2:** all 18 strings replaced, drafted from the delivered crops. 12 reuse the wording Lisa approved for the same photo on `/moving-to-elkhart/`, 2 are her own wording for the Lexington Avenue photo, 4 are new EN descriptions. The 6 remaining article pages have **no header image at all** — the 4 Wave 1 Spanish articles and the 2 location articles — and stay in PR 4 as planned.
- [x] **STANDING RULE — a version number in a changelog is data, not a pointer. For the Master Plan (Lisa, 2 Aug 2026).**
  > Genericizing stale version pins is not a find-and-replace. A pinned version in a **reference** ("built against Master Plan v2.8") is a pointer and goes stale; a pinned version in a **changelog or historical note** ("governing-documents line updated to v2.14") is a record of what was true at the time and must never be rewritten.

  Earned immediately: a first pass at the 15-file version sweep swept 22 files and rewrote, among others, the changelog entries in this very document and a header that lost its parenthetical form and read ungrammatically. Reverted; only the 15 approved-copy files changed. It is the same principle CLAUDE.md already applies to its own revision log, which never rewrites past entries.

- [x] **THE SIX STANDARDS FROM THE ELKHART BATCH — collected for the Master Plan.** Each is recorded in full in its own entry above; this is the index.
  1. **Source-repoint rule** — any source repoint must check whether existing alt text still describes the new image. Filename-stable replacement is invisible to an HTML diff.
  2. **Batch reconciliation** — source files minus rejections must equal job-table entries must equal built files, differenced set against set, never a running total. Built as `audit.js` check 22.
  3. **1:1 zoom sheets** — a contact-sheet tile is about a third of delivered size, so it under-represents fine detail by roughly 3×. Every batch review ships a companion sheet giving each legible-text region at native resolution.
  4. **Slug-to-string maps before drafts** — alt-text drafts are read against the image they are attached to, not only scanned for compliance, and the reviewer sees them in that form too. Backed by `audit.js` check 23.
  5. **Field-class coverage** — a content rule applies to every field class carrying that content, and a fix is incomplete until the parallel fields are verified.
  6. **Changelog versions are data** — the rule directly above.

- [x] **STANDING RULE — a content rule applies to EVERY field class carrying that content. For the Master Plan (Lisa, 2 Aug 2026).**
  > Any content rule applies to every field class carrying that content — visible copy, alt attributes, `og:`/`twitter:` equivalents, and schema fields. A fix applied to one field class is incomplete until the parallel fields are **verified**, not assumed.

  This is the fourth field class to sit outside every check, and it is now a rule rather than a fourth data point. The sequence: **body copy** (the Fair Housing sweep), then **alt text** (missed for months; check 17), then **meta descriptions** (check 18), then **`og:image:alt`** — where PR 2 corrected 18 `<img>` alt strings and left the parallel `og:` field describing the photo that used to be there. That field is what Facebook and LinkedIn read for a shared image, so the wrong description was live on social as well as on the page, and it stayed that way through a merge because the fix had been declared done after one field class. Fixed in PR 4, all 18 verified equal rather than assumed.
  **Enumerate before declaring done:** H1, body copy, alt attributes, meta description, `og:title`/`og:description`/`og:image:alt`, `twitter:` equivalents, JSON-LD schema fields, captions, button and CTA text, form labels.
- [x] **Section-image batch reconciliation — built as `audit.js` check 22, 2 Aug 2026.** Source files minus rejections must equal `SECTION_JOBS` entries must equal names built in `assets/images/sections/`, differenced set against set per source folder rather than trusting a running total. Added at Lisa's direction after `elkhart-moving-cta-closing` was reported as processed for several rounds and merged unprocessed in PR #104 — "27 uploaded, 25 processed, 1 rejected" was repeated without anyone subtracting. Proven against deliberately broken trees in all three directions (cleared source with no job entry, job entry with no built file, job entry with no source photo), with the true negative confirmed before and after.
- [x] **Contact sheets must carry 1:1 zooms of every legible text region — standard from 2 Aug 2026, for the Master Plan.** A contact-sheet tile renders at roughly **a third of delivered size**, so it under-represents fine detail by about 3×. That cuts both ways: a reviewer can wrongly reject a claim about text that is perfectly legible on the page, or wrongly approve alt text describing text no reader can actually resolve. It changed two decisions in the Elkhart living batch alone. **Every batch review from here ships a companion 1:1 sheet** giving each legible-text region at native resolution with the pixel dimensions it occupies in the delivered file, so a text claim is checked against what renders rather than inferred from a tile. First built as `docs/CONTACT-11-living-text-1to1.png`.
- [ ] **Alt-text drafts must be checked against the image they are attached to, not only for compliance.** Approved string #9 of the PR 3 set described "a river bend seen from a park bank"; its image is a county fair midway at dusk. The string was compliant, well-formed, and about the wrong photograph, and it passed a review gate that way — every check run on it tested wording, not correspondence. Caught while building the wiring map, when the slug-to-string assignment was laid out explicitly. **Standing practice: lay the slug-to-string map out explicitly and read each string against its own rendered image before sending drafts for approval.** Worth considering as a mechanical check — an alt string that shares no content word with its slug is a cheap heuristic and would have flagged this one.
- [ ] **`npm run --clean` cannot complete — pre-existing, not introduced by the Elkhart work.** `generate_clean_from_composite("does-lisa-collio-speak-spanish")` raises: it expects a 1200×900 Template B composite and the file on disk is already the derived 1200×630, so the source it needs no longer exists. **Verified pre-existing** by running the unmodified pre-Elkhart script from the repo root and getting the identical error. Consequence: the whole-table rebuild the `CLEAN_JOBS` reproducibility guarantee depends on is currently broken for every slug after that one alphabetically. The 24 Elkhart slugs were built by iterating their own `CLEAN_JOBS` entries through `generate_clean()` — same function, same convention, just scoped past the broken row. Fix needs a decision on where that one image's true source lives.
- [x] **Amtrak route names — both live pages FIXED 2 August 2026.** The Capitol Limited no longer exists: merged with the Silver Star into the **Floridian** on 10 November 2024. Elkhart station (EKH, **131 Tyler Avenue**) is served today by the **Lake Shore Limited** (Chicago ↔ New York City / Boston) and the **Floridian** (Chicago ↔ Miami, via Washington DC), and sits across the tracks from the National New York Central Railroad Museum. Sources: Amtrak's EKH station page, Wikipedia "Elkhart station" and "Floridian (train)", the Nov 2024 Floridian timetable. The approved ¶2 rewrite removed both occurrences and names **no route at all**, deliberately, so it cannot go stale again. **Zero live pages now name the Capitol Limited** — verified by grep across every HTML file, not assumed.
- [ ] **Two authoring sources still name the Capitol Limited — NOT fixed, on Lisa's instruction.** `docs/approved-copy/V1_0_Moving_to_Elkhart_Pillar_EN_Lisa_Collio.md`:81 and `docs/approved-copy/V1_0_ES_Mudarse_a_Elkhart_Page_Lisa_Collio.md`:67, both holding the pre-rewrite ¶2. Reported first; may fold into the drive-time PR. This is build-procedure failure 4 exactly — a correction lands on the page while the approved source keeps the retired fact, so the next rebuild reintroduces it.
- [ ] **Goshen-to-Elkhart drive time: the site states two different numbers, and `VERIFIED_FACTS.md` states both.** 25 minutes on the location article and everything echoing it; 20 minutes on the three Elkhart pillar pages. `docs/VERIFIED_FACTS.md` §Elkhart — location says "About **25 minutes** northeast of Goshen" and then, in the next sentence, "South Bend, Mishawaka, and Goshen are all within roughly **20 minutes**." **Fix `VERIFIED_FACTS.md` first, then each page against it** — never page-against-page. Full inventory of every occurrence in both languages is in `docs/ELKHART_IMAGE_PR1_PREFLIGHT.md` §drive-time sweep. **Not touched — Lisa is confirming the right number, then one PR.**
- [ ] **Remaining site-wide placeholders** — community, client, and family photos per the Photo Shot List.
- [x] **Twelve hero photographs — DELIVERED AND WIRED, 13 August 2026.** All twelve real photographs replaced the shared placeholder on all 24 pages (EN+ES). Source: Lisa uploaded via GitHub's web UI to branch `lisa/hero-photos`; processed via `scripts/process-hero-images.py` (renamed from `generate-hero-placeholder.py`, which now also owns the real-photo pipeline and the updated `HERO_JOBS` table with per-slot source blob SHA, delivered dimensions, crop behavior and provenance). `audit.js` check 26 flipped from WARNING to ERROR in the same PR — the condition recorded below for that flip is now met.
  **Four files were Apple MPO containers (Live Photo bundles), not plain JPEG** — `moving-to-goshen.jpg`, `moving-to-elkhart.jpg`, `living-in-elkhart.jpg`, `about.jpg`. Re-saved as plain JPEG by the processing script (Pillow reads the primary frame, discards the trailing MPO data). Real format defect, not a preference — fixed, not just noted.
  **Three files ship under the 2400px floor — Lisa's ruling, 13 August 2026: ship as-is, do not block, log as post-launch replacement candidates.** `blog.jpg` (1813px), `contact.jpg` (2038px), and the Sellers photo `twilight-front-1.jpg` (2038px) — will read slightly soft on a large monitor, fine on the phone traffic that is most of Lisa's visitors. Not re-shot for this PR. `market-stats.jpg` (2418px) clears the floor by only 18px — not flagged for replacement, but has essentially no margin if ever re-compressed.
  **Provenance rulings, Lisa, 13 August 2026** (full detail in `content/source/licensed-assets.txt`, created this PR): `home.jpg`, `buyers.jpg`, `next-chapter-method.jpg` are Lisa's own listing photography with stripped EXIF — confirmed hers, not a rejection signal. `buyers.jpg` and `next-chapter-method.jpg` are the same property (approved). Sellers' `twilight-front-1.jpg` is third-party licensed — shot by a photographer Lisa hired, she holds the license and the sellers' agreement; the property (1765 N Bay Drive, Elkhart) was already in `content/source/sold-listing-consent.txt`, so no new consent entry was needed there.
  **Standing policy change, Lisa, 13 August 2026 — recorded here since it changes how future sessions review imagery, not just this batch:** *"Do NOT ask about legible signage, dated content, or crop composition again unless it is a legal problem. Several of the standards this project accumulated around imagery are aesthetic conventions, not compliance rules, and they have been generating rework with no return. Only three things gate an image now: Fair Housing, rights to use it, and whether it is Lisa's to publish."* This ruling followed a Phase 0 measurement report that flagged legible marquee text, a business sign, a phone number and two house numbers on the twelve photos — all of which Lisa approved as-is. **This is a real narrowing of prior practice** (the Hero Standard and several Master Plan sections treat legible signage/business names/crop survival as findings in their own right); recorded here rather than only in chat so the next session doesn't re-litigate it, but the governing documents themselves are Lisa's to amend and are not edited by this entry.
  **Deferred item CLOSED 17 August 2026 — see the new item directly below for the fix actually shipped.** The Tier 2 hero rendered 423px mobile / 376px desktop; now 551px mobile / 488px desktop.
- [x] **Hero focal-point mechanism + seven crop adjustments + three photo replacements + Tier 2 height fix — DELIVERED, 17 August 2026 (branch `claude/hero-focal-point-and-crops`).** Report-Before-Build: Phase 0 report identified the underlying mechanism gap (hero background crops dead-centre at every viewport, no way to bias it, unlike the section-image pipeline's `focal_y`) and proposed `--hero-focal-x`/`--hero-focal-y`; Lisa approved "with both axes, as you designed" plus all fourteen focal numbers plus the larger height option.
  **Mechanism:** `--hero-focal-x` / `--hero-focal-y` custom properties on `.hero--photo` (`assets/css/src/site.css`), used via `background-position: var(--hero-focal-x, 50%) var(--hero-focal-y, 50%)`, replacing the old hardcoded `background-position: center`. Both default to `50%` so no page changes unless it opts in — same pattern as `--hero-img`. Set per page via `build.js` marker JSON (`heroFocalX`/`heroFocalY` added to `DEFAULTS` and to `components/hero-tier2.html`'s inline style) for the eleven Tier 2 pages, or inline style directly for Home (Tier 1, raw HTML, not marker-based).
  **The axis-flip finding — the substance of the Phase 0 report, per Lisa.** `background-size:cover` crops whichever axis the container overflows on relative to the source photo's own aspect ratio, and that axis **flips between viewports** for Tier 2: at desktop, containers are wider-than-tall relative to these photos, so `cover` crops height and `--hero-focal-y` controls framing; at mobile, the same containers are narrower-than-tall, so `cover` crops width instead and `--hero-focal-x` does the work — `-y` has no visible effect there. A "crop up/down" instruction only addresses desktop unless a matching `-x` value is set too. A vertical-only focal property would have fixed desktop and done nothing on mobile, where most of Lisa's traffic is, and would have looked correct to anyone reviewing on a laptop. **Recorded in both places Lisa named:** `docs/approved-copy/V1_0_Hero_Standard_Lisa_Collio.md` §3 (the old "no focal-point control" line rewritten) and here.
  **Seven focal-point adjustments** (all fourteen numbers — EN+ES — approved as proposed): `home` x=58% y=25% (crop up, house not lawn) · `blog` x=50% y=30% (crop up toward house, was showing driveway/yard) · `about` x=50% y=18% (crop up so the RE/MAX Results sign reads, was centered on the window) · `contact` x=50% y=75% (crop down onto house, was mostly sky) · `moving-to-goshen` x=32% y=28% (favor buildings, not street/cars) · `living-in-goshen` x=50% y=65% (set after reporting what the new photo shows — courthouse + flowerbed, crops down slightly off sky) · `moving-to-elkhart` x=50% y=68% (favor flowerbed/buildings, not sky). The other five hero slots (buyers, sellers, living-in-elkhart, next-chapter-method, market-stats) keep the 50%/50% default — not part of Lisa's adjustment list. Full per-slot record, including the reason for each value, is in `HERO_JOBS` (`scripts/process-hero-images.py`).
  **Three photo replacements**, same filenames as the files they replace, per Lisa's instruction (`assets/images/hero/moving-to-goshen.jpg`, `living-in-goshen.jpg`, `moving-to-elkhart.jpg`): Lisa's own iPhone photographs, uploaded to `lisa/hero-crop-fixes` and confirmed as genuinely new content via git blob SHA comparison against the files they replace (not a re-upload under the same name). All three were Apple MPO containers (Live Photo bundles) again, handled transparently by the existing `process_one()` pipeline — same fix as the four MPO files in the original twelve-photo batch, not a new defect. Processed to 3200×2400 plain JPEG + WebP. **Alt-text re-verification requested by Lisa, and reported rather than assumed:** hero backgrounds are pure CSS (`--hero-img` on a `<section>`, no `<img>` tag) — confirmed via a repo-wide grep that zero `alt=` attributes reference any `assets/images/hero/` path, so there is no alt text to re-verify. The `og:image:alt` fields on the three replaced-photo pages point at a different, unrelated file family (`assets/images/blog-headers/*-header.jpg`, the featured-image/OG system, not hero backgrounds) — checked and correctly out of scope, not overlooked.
  **Height fix**, bundled per Lisa's instruction (component CSS, same file as the focal-point work): `.hero--tier2 { padding-block: 7rem 0; }` / `.hero--tier2 .hero__text { padding-bottom: 8rem; }`, replacing the prior padding. Measured via Playwright against the real component (not just CSS reasoning): 551px mobile / 488px desktop, an exact match to the prototype Lisa approved, identical whether the background is a placeholder or a real photo (padding-driven, not image-driven). Still well short of Home's Tier 1 hero (967px/716px), deliberately — Tier 2 carries no portrait to fill that height with.
  EN and ES ship in the same PR (all fourteen focal values cover both languages per slot; the three replacement photos are shared files, not per-language).
- [x] **Home page body images (five) + AI-illustration policy change — DELIVERED, 17 August 2026.** Report-Before-Build: Lisa supplied five files on branch `lisa/home-body-images` — two of her own photographs, three AI-generated illustrations (the site's first) — with filenames matching the Home page sections they belong under.
  **Location problem, found before anything else and fixed, not silently:** the branch did not actually contain `assets/images/home/` — GitHub's web "create file" flow produced a single blob file literally named `home` (git does not track empty directories), and the five images landed at the repo root instead. Flagged in the Phase 0 report rather than treated as expected; approved and fixed by moving the five into a real `assets/images/home/` directory and deleting the stray root-level blob. **Worth a permanent note: Lisa will upload this way again, and GitHub's create-file flow will do the same thing every time** — a future session should expect this exact failure mode, not be surprised by it.
  **Section mapping, verified against both live pages, not assumed:** Home EN and ES have the same five target sections in the same order (`lisa-collio-knows.jpg` → "Lisa Collio knows this market"/"...conoce este mercado"; `next-chapter-method.jpg` → the Next Chapter Method teaser; `real-estate-guidance.jpg` → "Real Estate Guidance You Can Trust"/"Acompañamiento en el que puede confiar"; `transforming-dreams.jpg` → Buyers path card; `unlocking-smart-moves.jpg` → Sellers path card). No structural mismatch between languages; same image serves both, alt text differing per language, the site-wide pattern.
  **Two markup patterns, matched to precedent already live on the page, not invented:** the three Tier-1-style sections use `.section-figure` (`<picture>` + WEBP source, matching `living-in-goshen/index.html` and other community pages); the two path-card images use `.local-card__img` (JPEG only, no WEBP — matching the existing Goshen/Elkhart path-card images already on this same page). WEBP siblings generated for the three section-figure images only, per Lisa's instruction.
  **Resolution — `real-estate-guidance.jpg` ships at 957×958, under the 1200px floor.** Lisa's ruling, 17 August 2026, made knowing the shortfall: will read slightly soft on a large monitor, fine on the phone traffic that is most of her visitors. **Logged here as a post-launch replacement candidate**, same pattern as the three under-floor hero photos above.
  **Filename collision, real and worth carrying in both build records (done — see `content/source/licensed-assets.txt` and `scripts/process-home-body-images.py`):** `assets/images/hero/next-chapter-method.jpg` (Lisa's own listing photo, the Next Chapter Method page's hero) and `assets/images/home/next-chapter-method.jpg` (the new AI-generated illustration) share a base filename. Different folders, different content, different purpose — never cross-reference the two.
  **Policy change — the rule was amended, not the images excepted, per Lisa's explicit instruction.** Hero Standard §6 ("Original photography only. No stock, no AI-generated places or people") and the Master Plan's Images section ("Lisa's original photography ONLY. No stock or AI-generated property/people/place images") both banned AI imagery outright — the second one site-wide, not hero-scoped, and the one that actually governed body images. Swept every governing document first, per Lisa's instruction, exactly because a rule fixed in one place and left standing in a sibling document is a pattern this project keeps hitting — found both bans, and confirmed CLAUDE.md's ASSETS section mirrored neither (a pre-existing gap, not a duplicate needing removal). Master Plan bumped to v2.22 (v2.21 deleted): AI-generated illustrations now permitted as body images, broker-approved, where they aid comprehension, never as a substitute for a real photograph, never depicting a real place/person/property that could be mistaken for a listing, recorded as an illustration in `content/source/licensed-assets.txt`. Hero Standard bumped to v1.1: one clarifying sentence added to §6 so the body-image exception is never generalized upward to heroes. CLAUDE.md bumped to v2.6, gaining the rule it never had.
  **Broker approval — verbal, not written, cited as such rather than implying a document exists:** "verbal approval from the managing broker, recorded by Lisa, August 2026." Per Lisa's instruction, the `licensed-assets.txt` entries state plainly that no written artifact exists, and are updated if the broker's single comprehensive pre-launch review later produces one covering this.
  **`licensed-assets.txt` gained a `TYPE` and `DOES NOT DEPICT` field**, added because forcing AI-generated content into photograph-shaped fields (PHOTOGRAPHER, EXIF NOTE) would have been the wrong shape — `DOES NOT DEPICT` is the field that actually answers the question a future reader will have. Four entries added: the three illustrations, plus `real-estate-guidance.jpg` (a real photograph with no EXIF, extending the same missing-EXIF-needs-a-positive-record pattern the file already exists for — not explicitly requested, flagged as an addition in the PR).
  **Alt text, ten strings (five images × two languages), Lisa's approval required before shipping and obtained:** each describes what is actually depicted — the illustrations described as illustrations, never implied to be photographs of real places or people, so neither a screen reader nor an AI answer engine reads them as something they are not.
  **Hero Standard's own stale status line, corrected while editing the same document for an unrelated reason:** it read "Status: DRAFT — pending Lisa's approval. Not built" since 12 August, five days after Lisa actually approved it and twelve hero photographs built from it went live. Corrected to "APPROVED BY LISA — 12 August 2026. Built," and §10's closed "Eleven Tier 2 photographs" open item cleared (struck through, not deleted, so the section's own history stays visible). Same class of defect as the Market Stats row and the cutout-scope passage in Master Plan v2.21 — a governing document describing a state of the site that had stopped being true.
  EN and ES ship in the same PR (all ten alt strings cover both languages; the five image files are shared, not per-language).
  **`lisa-collio-knows.jpg` cropped, same PR, on Lisa's review of the preview.** Source was 3024×4032 portrait with substantial empty ceiling/floor around the subject. Report-Before-Build applied at pixel-boundary grain: proposed crop box `(left=65, top=1010, right=3010, bottom=3130)` — banner top to just below Lisa's feet, full balloon arch — reported with resulting dimensions before touching the file; Lisa approved as proposed. Delivered **2945×2120**, well clear of the 1200px floor. Alt text (EN/ES) re-verified against the cropped frame and left unchanged — the crop removes empty space only, not any element the alt text describes. `HOME_BODY_JOBS` build record updated with the crop parameters; source blob SHA unchanged (crop is a processing parameter on the same upload, not a re-upload). Lisa flagged, not as a blocker, that the crop keeps the RE/MAX Results banner at full height/prominence in a section about her — she'll assess on the live preview, and a tighter second crop (not a reshoot) is the fix if she wants one.

## B. In flight — awaiting merge

- [x] **PR #129 — Hero copy v1.2, six Spanish headline revisions, `docs/approved-copy/V1_2_Hero_Copy_Tier_2_Lisa_Collio.md` (supersedes v1.1, deleted in the same change). Merged 18 August 2026.** Report-Before-Build confirmed all six live strings matched the document's recorded v1.1 values exactly (no drift), confirmed the two Moving pages genuinely share one string, and confirmed no other field on any of the six pages — meta description, `og:title`, `twitter:title`, JSON-LD, internal link text — carried any of the six headlines. No English page touched.
- [x] **Blog listing manifest + generator — DONE, both stages, 18 August 2026.** `/blog/spanish/` is restructured to mirror `/blog/`'s shape (topic-card grid + flat all-articles list) instead of its own six plain `<h2>` sections — Lisa's ruling, superseding the earlier "use the ES structure as it is" ruling, because she plans to keep adding topics over time and two differently-shaped pages means designing every future topic twice. Cross-posting (one article under more than one topic) is a supported feature now, not a workaround.
  **New data sources:** `content/blog-articles.json` (112 entries — all 57 EN + all 55 ES articles, backfilled from the live pages, each with a `categories` array) and `content/blog-topics.json` (5 EN topics, 8 ES topic entries — the six existing ES sections, a new Reubicación, and an Actualizaciones del Mercado entry that renders nothing while empty). Deliberately **not** stored in the manifest: thumbnail existence and alt text. Both already have exactly one canonical home — the article's own page and the filesystem — and a third copy in the manifest is precisely the field-class duplication the Alt-Text Correspondence Standard exists to prevent. `build-blog-listing.js` reads each article's own page for its alt text and checks disk for its thumbnail, every run.
  **The EN asymmetry, found during the prior blog-images Phase 0 report and fixed here as its own line item, not folded silently into the restructure:** `moving-to-elkhart-indiana-from-out-of-state` and `working-in-the-rv-industry-and-buying-a-home-in-elkhart-indiana` were categorized `community` while their Goshen counterparts were categorized `relocation` — same subject, same shape, inconsistent topic. Pre-existing, not introduced by this work. Fixed as a **listing-only recategorization** in the manifest — canonical URLs, sitemap entries, and hreflang pairs for both articles are untouched, so there is no redirect risk and nothing that could break a link or a search index entry. EN's Relocation topic count moves from 2 to 4; Community from 32 to 30.
  **Cross-posting, made concrete:** the four ES relocation-shaped articles (`industria-rv-comprar-casa-goshen-indiana`, `mudarse-a-goshen-desde-otro-estado`, `trabajar-en-la-industria-de-rv-y-comprar-casa-en-elkhart-indiana`, `mudarse-a-elkhart-indiana-desde-otro-estado`) carry two categories each — their original city section, plus `reubicacion` — and the generator renders their card once per category. They stay in Goshen's and Elkhart's blocks (11 and 12 articles, unchanged) and also appear under the new Reubicación topic (4 articles). 55 unique ES articles render as 59 cards on the page once Stage 2 applies. Reubicación's category-card image is `assets/images/elkhart/elkhart-moving-location-commute.jpg` — the same file already approved for EN's Relocation card, reused deliberately for cross-language identity, checked again against `rejected-assets.txt` and confirmed clear.
  **Market Updates in Spanish — confirming the finding plainly, since it was misread once already:** no Spanish market-update blog article exists. "Los números, sin adornos" is `/es/estadisticas-del-mercado/`'s page hero headline (shipped in PR #129), not a blog article — the two are unrelated pieces of copy that happen to be near each other in this project's recent history. The Actualizaciones del Mercado topic entry exists in `blog-topics.json` for when a real article arrives; the generator omits any topic from the rendered grid whose computed article count is zero, so it renders nothing today. This is a general rule (any topic that empties out later disappears the same way), not a special case written for this one topic.
  **Reconciliation, by set difference, not a running total, per the Batch Reconciliation Standard:** manifest slugs vs. real `blog/**/index.html` directories on disk — zero orphans either direction, both languages. Pre-migration rendered slug count vs. manifest count — **55 in, 55 accounted for; 57 in, 57 accounted for.** Spot-checked title/description extraction against tricky cases (curly quotes, ™, accented characters, ¿) for corruption — none found; every field was extracted programmatically from the live pages, never hand-retyped, so the only field this change actually alters is `categories` on six articles total (four ES cross-posted, two EN recategorized).
  **Stage 1 (PR #130, merged) added the data and generator only** — `build-blog-listing.js --preview` wrote to `docs/drafts/`, neither live page was touched, full pipeline verified clean.
  **Stage 2 applied it for real, after Lisa's merge of Stage 1 stood as the review approval.** Marker comments (`blog-topics:start/end`, `blog-articles:start/end`) added to both live pages — EN's markers wrap only the five real topic cards, deliberately excluding the pre-existing "En Español" language-switch card (not a topic, kept hand-authored outside the generated region); ES's entire six-`<section>` structure was replaced with the same two-section skeleton EN already had, since ES had no equivalent shape to slot markers into. `--apply` run, then re-verified against the live output, not just trusted from the dry run:
  - **Reconciliation re-run against the actual applied pages, not the preview:** manifest vs. rendered article-detail links — zero missing, zero extra, both directions, both languages. Rendered ES card count: **59**, exactly the 55 unique + 4 cross-posted repeats predicted in Stage 1.
  - **Content-stability check re-run against the applied pages:** every EN article's title/description/URL diffed against the pre-migration page, article by article — zero unexpected changes found; the only diff anywhere in the comparison was the Relocation topic card's own description text (updated to say "Goshen or Elkhart," since the topic now covers both), which was the one deliberate change.
  - **A real, useful `npm run audit` catch:** existing check 8 (`blog-counts`) compared each topic card's stated count against a literal directory listing under `blog/{category}/` — an assumption the EN listing-only recategorization breaks by design (the two Elkhart articles' *files* still live under `blog/community/`, only their *listing* moved). Rewrote the check to count from `content/blog-articles.json` instead of folders, and extended it to also verify ES's seven topic counts (previously unchecked) — re-run and confirmed true-negative on the corrected pages, and would still catch a real mismatch (verified by reasoning through what folder-based counting would have wrongly flagged forever going forward, not just this once).
  - **A real generator bug, found by testing before committing, not assumed away:** the first `--apply` run looked correct, but a second run on the same data produced a different file — a trailing blank line accumulating between the injected content and the `:end` marker each time, because the marker regex only consumed one optional newline before the close tag instead of all the whitespace left there. Caught by explicitly testing idempotency (apply twice, diff the result) before trusting the script, not by assuming a script that worked once would keep working. Fixed by widening the regex to consume all whitespace before the end marker; re-tested twice more, byte-identical both times.
  - `content/image-parity-baseline.json`'s `/blog/spanish/` pair re-measured fresh against the actually-applied page — **52**, measured directly (`grep -c "<img "`), not carried forward from any earlier stage's count or calculated by hand. The prior 47 was set before this restructure existed; the two numbers describe different page structures and aren't meant to reconcile against each other.
  Full pipeline clean after all of the above: `build.js --check`, `hash-assets.js` (no further changes — the generator's own hashing already matched), `build-reviews.js --check`, `npm run audit` (one pre-existing, unrelated SEO warning).
  **The standing lesson, in Lisa's words: Spanish copy that passes a grammar check can still be translated English.** Every one of the five replaced strings was structurally English wearing Spanish words — correct grammar, wrong instinct. The test is not whether a line is correct, it's whether a Goshen seller would actually say it, and that test only works applied by someone reading the line as Spanish on its own, not by someone comparing it to the English line it sits beside. The clearest case: *"Cada mudanza es un capítulo"* used *mudanza* (the physical act of moving boxes) where the English "move" meant a life decision — the Life Chapters page read as "every truckload is a chapter." Two Spanish headlines were reviewed and deliberately kept, so a future session doesn't assume the whole set was suspect: Contact's *mudanza* is correct there (that page is about the move itself), and the Living pages' longer line stands because it's long but natural, not translated-sounding.
  **The field-class sweep came back clean, and it's worth recording why, not just that it did.** Each headline lives in exactly two places on its own page — the `build:hero-tier2` marker JSON and the rendered `<p class="hero__promise">` — and nowhere else on the page or the site. That's a clean field class, and it's clean *because* the hero is a shared partial (`components/hero-tier2.html`) rather than hand-duplicated markup: one component, one place the string is authored, no independent copies to drift out of sync. Worth contrasting with the pattern this project has hit repeatedly elsewhere — the Field-Class Compliance Coverage Standard exists because the last several copy and compliance sweeps on this site (Fair Housing terms, `og:image:alt`, the reviews-version pointer) each found the same string scattered across meta tags, schema fields, and hand-authored markup that a componentized field never has to re-litigate. A partial doesn't make the *content* correct — that still needed a human ear for translated-sounding Spanish — but it means fixing the content once was actually enough.
- [x] **EN blog topics — Community retired entirely, DONE 18 August 2026.** Change of direction from Lisa, superseding the plan that kept Community alongside new topics: Community is gone, not narrowed. All 32 articles physically under `blog/community/` get a new home across four new topics — Meet Lisa, Goshen, Elkhart, Life Chapters — none of them a general bucket. English now carries 8 topics total (Buyers, Sellers, Meet Lisa, Goshen, Elkhart, Relocation, Life Chapters, Market Updates), matching Spanish's 8 (Comprar, Vender, Conozca a Lisa, Goshen, Elkhart, Reubicación, Tu Próximo Capítulo, Actualizaciones del Mercado) topic-for-topic.
  **Scope, checked before classifying anything:** 32 directories live under `blog/community/` on disk, but only 30 carried `categories: ["community"]` in the manifest — the other 2 (`moving-to-elkhart-indiana-from-out-of-state`, `working-in-the-rv-industry-and-buying-a-home-in-elkhart-indiana`) were already moved to `relocation` by the EN-asymmetry fix two entries above. Flagged to Lisa rather than assumed; her ruling: those 2 are already correctly homed and out of scope here — left untouched.
  **Classification of the 30, by title, reported before building and built exactly as approved:** Meet Lisa (7) — `does-lisa-collio-speak-spanish`, `how-many-homes-lisa-collio-sold-goshen-elkhart`, `lisa-collio-helps-seniors-families-downsize`, `what-is-it-like-to-work-with-lisa-collio`, `what-makes-lisa-collio-different`, `why-clients-choose-lisa-collio`, `why-lisa-collio-became-real-estate-agent`. Goshen (8) — `what-is-goshen-indiana-known-for`, `goshen-indiana-school-district`, `downtown-goshen-indiana`, `goshen-indiana-industries-employers`, `goshen-indiana-landmarks-amenities`, `things-to-do-in-goshen-indiana`, `community-events-in-goshen-indiana`, `cost-of-living-in-goshen-indiana`. Elkhart (10) — `what-is-elkhart-indiana-known-for`, `elkhart-indiana-school-districts`, `elkhart-indiana-industries-employers`, `elkhart-indiana-landmarks-amenities`, `elkhart-indiana-location-nearby-cities`, `things-to-do-in-elkhart-indiana`, `community-events-in-elkhart-indiana`, `cost-of-living-in-elkhart-indiana`, `buying-an-older-home-in-elkhart-indiana`, `downtown-elkhart-indiana`. Life Chapters (5, flagged as not cleanly fitting any of the other three — not city-specific, not "about Lisa" in the background/credentials sense — Lisa's ruling: give them their own topic to mirror Spanish's existing Tu Próximo Capítulo rather than force them into Meet Lisa) — `what-is-the-next-chapter-method`, `next-chapter-method-different-from-typical-process`, `does-next-chapter-method-cost-extra`, `how-long-does-next-chapter-method-take`, `not-sure-which-life-chapter`.
  **Arithmetic verified programmatically before building, not trusted from either side's mental math:** Lisa's own check-your-math note caught something real — her count read "30 classified + 5 now assigned to Life Chapters = 35, plus the 2 already-homed = 37," which double-counts the 5 Life Chapters articles (they were already inside the 30 classified, not additional to it). Correct total: 7 + 8 + 10 + 5 + 2 already-relocated = **32**, verified against the actual `blog/community/*/` directory count (`ls -d` — 32) before any file was touched.
  **Image picks, each checked against `rejected-assets.txt` at selection time (none listed):** Meet Lisa → `assets/images/lisa/lisa-collio-headshot-red-top.jpg` — same file already approved for Spanish's Conozca a Lisa. Elkhart → `assets/images/elkhart/elkhart-living-downtown.jpg` — same file as Spanish's Elkhart section. Life Chapters → `assets/images/hero/next-chapter-method.jpg`, per Lisa's instruction to check the `/next-chapter-method/` page's own images first — this is that page's own hero photo, and also the file already approved for Spanish's Tu Próximo Capítulo. Each language wrote its own alt text for these shared files rather than sharing one, matching the existing Relocation/Reubicación precedent.
  **Goshen's pick was swapped before merge, per Lisa's review of PR #132 (18 August 2026).** Originally `assets/images/goshen/goshen-living-downtown.jpg` (the file Community had used, carried straight over) — Lisa asked instead for `assets/images/hero/moving-to-goshen.jpg`, the downtown streetscape already approved and live as the `/moving-to-goshen/` page's own Tier 2 hero. Checked against `rejected-assets.txt` (not listed) and confirmed present on disk (3200×2400). This is a deliberate dual-purpose reuse of one file across two different placements — a Tier 2 hero background and a blog topic card — the same pattern already used elsewhere on the site (e.g. `hero/next-chapter-method.jpg` above). Fresh EN alt written since the hero itself carries no `<img>` alt to reuse (CSS background image, decorative).
  **Corrected the same day: Lisa asked for the matching swap on the Spanish side too** — this was field-class incomplete on first pass, changing only the EN entry when the same file is used by both languages' Goshen topic card. Spanish's `goshen` entry now also uses `assets/images/hero/moving-to-goshen.jpg` (3200×2400), with its own freshly-written Spanish alt — `"Una calle del centro de Goshen, Indiana."` — not a translation of the English alt, matching the standing rule for shared files across languages (Relocation/Reubicación, Life Chapters/Tu Próximo Capítulo). **Both Goshen topic cards now match again**, this entry's earlier "Goshen is now the one topic whose EN and ES images don't match" note is superseded. Confirmed by grep that `goshen-living-downtown.jpg` is no longer referenced by either language's `blog-topics.json` entry — the only remaining references to that file are `living-in-goshen/index.html` and its ES twin, unrelated pages, not blog topic cards.
  **A real generator gap, found and fixed by this topic set, not present before:** `build-blog-listing.js`'s flat-list category label for English cards linked to `TOPICS.en.find(...).hubUrl` unconditionally — every EN topic before this one had a real hub page, so a `null` `hubUrl` was never exercised. Meet Lisa, Goshen, and Elkhart have no dedicated hub page (mirroring Spanish's own `null`/anchor-link precedent for the same three), which would have rendered the literal string `"null"` as a link target. Fixed with the same `t.hubUrl || `#${t.key}`` fallback the topic-grid renderer already used — verified in the applied output that all three link to their in-page anchors correctly.
  **Reconciliation, by set difference:** manifest EN slugs vs. `blog/**/index.html` directories on disk — 57 vs. 57, zero orphans either direction. `git diff content/blog-articles.json` showed only `categories` array values changed on the 30 affected articles — no title, description, or URL touched, confirmed by grep rather than assumed. New category counts: buyers 10, sellers 12, meet-lisa 7, goshen 8, elkhart 10, relocation 4, life-chapters 5, market-updates 1 = 57. ✓. Generator re-run twice after `--apply`, byte-identical both times (idempotent).
  **`content/image-parity-baseline.json`'s `/blog/` pair re-measured fresh** against the actually-applied page — **64** (`grep -c "<img "`), up from 61 (net +3: four new topic images in, one retired Community image out is +3 not +4, since the category card structure holds one image per topic regardless of count). Spanish's 52 is unchanged — this was an English-only restructure. `npm run audit` clean after the rebaseline (one pre-existing, unrelated SEO warning, same as before this task).
  **The `hubUrl` loose end, logged per Lisa's ruling 3, not fixed:** `blog-topics.json`'s old `community` entry pointed to `/blog/community/`, a real hub page that still exists at that URL with all 32 articles still linked from it. Retiring the topic card only removes the link *from* `/blog/`; the page itself is untouched and stays live, now reachable only by direct URL or internal links from the article pages themselves. Whether to redirect it, repurpose it, or leave it as an orphaned-but-reachable page is Lisa's call, not made here.
  **Found but not touched, flagging rather than fixing mid-task:** this file's own item above (the "Blog listing manifest + generator" entry, lines 155–170) has two sub-bullets — the "standing lesson" about translated-sounding Spanish and the "field-class sweep came back clean" note — that read as PR #129's (Hero Copy v1.2's) content, not the blog-listing work they're nested under. Looks like a misplaced block from an earlier edit, not something this task caused. Left alone since fixing it isn't in scope here and touching that entry's structure without checking against Lisa's original wording risks the exact kind of silent-deletion mistake this file's own governing standard warns about.
  Full pipeline clean: `build.js --check`, `hash-assets.js` (no changes — hashes already matched), `build-reviews.js --check`, `npm run audit`.
- [x] **First Spanish Market Updates article — DONE 18 August 2026.** `blog/spanish/actualizacion-del-mercado-elkhart-county-mitad-2026/`, built from `docs/approved-copy/V1_0_ES_Market_Update_Mitad_2026_Lisa_Collio.md` (Lisa-approved before building, committed to the repo alongside the page per the commit-before-use rule). Same verified ECBOR data set as the EN `elkhart-county-mid-year-market-update-2026` article and `/market-stats/` — no new figures introduced or recomputed. Cultural adaptation per Volume 37, not a translation: drafted from the numbers up in usted register, not derived from the English article's sentence structure.
  **Report-Before-Build confirmed, before touching anything:** slug/URL collision — none, checked against disk and the manifest. `blog-topics.json`'s `es.market-updates` entry — already existed (added when the ES manifest was first built so the topic could sit empty), not missing; all fields were `null` and needed populating, not a new entry needing creation.
  **No hreflang pair, deliberately** — this is the first Spanish Market Updates article and has no genuine English twin any more than the English mid-year update has a Spanish one; the two are parallel market updates, not a translated pair. Added `/blog/spanish/actualizacion-del-mercado-elkhart-county-mitad-2026/` to `audit.js`'s hreflang exempt list, same reasoning as the existing EN exemption for its counterpart.
  **Image: `assets/images/hero/market-stats.jpg`**, already approved and in use as both language's `/market-stats/`/`/es/estadisticas-del-mercado/` hero and the EN Market Updates topic card. Checked against `rejected-assets.txt` — not listed. Fresh Spanish alt written for this placement, not a translation of the EN card's alt text (`"Aerial view of a Goshen, Indiana residential neighborhood."`) — `"Casas y calles vistas desde el aire en una zona residencial de Goshen, Indiana."`, describing the same scene with its own construction, matching the standing rule from the Goshen swap above.
  **The topic card's render confirmed against the actual applied `/blog/spanish/` page, not the code path, per Lisa's instruction that this was the entire point of the PR:** `grep` on the built file shows the "Actualizaciones del Mercado" card present with the market-stats image, linking to `#market-updates`, count "1 artículo" — and the flat-list article card carries the matching `id="market-updates"` anchor. Before this article existed the card was correctly absent (0 count); confirmed present now that count is 1.
  **Reconciliation:** manifest ES slugs vs. `blog/spanish/*/` directories on disk — 56/56, zero orphans either direction. EN and ES `market-updates` category counts both now read 1. Generator re-verified idempotent (`--apply` run twice, byte-identical).
  **Three knock-on fixes this article's arrival surfaced, all real, not scope creep:** (1) `blog/index.html`'s hand-authored "En Español" card count was stale at 55, corrected to 56. (2) `llms.txt` regenerated (`npm run llms`) to include the new page. (3) `audit.js`'s `IDENTITY_COUNTY_OK` exemption list needed the new title's exact county-descriptor phrase (`"Elkhart County, Mitad de 2026"`) added, the same pattern as the existing EN market-update exemption — a real, dated, county-level MLS report is exactly what that exemption exists for, not a loosening of the "Elkhart County" ban.
  `content/image-parity-baseline.json`'s `/blog/spanish/` pair re-measured fresh at **53** (was 52; `grep -c "<img "` against the actually-applied page, not calculated). `/blog/`'s 64 is unchanged — this was a Spanish-only addition.
  Full pipeline clean: `build.js --check`, `hash-assets.js` (no changes), `build-reviews.js --check`, `npm run llms -- --check`, `npm run audit` (one pre-existing, unrelated SEO warning).
- [x] **Buyers page images — DONE 18 August 2026.** `/buyers/` and `/es/compradores/` gain their first body images: 2 real photographs, 7 AI-generated process-step illustrations (one, `get-pre-approved-first.png`, carries baked-in text), and 4 AI-generated "why work with Lisa" cards per language (8 total, EN + ES), broker-approved verbally, same basis as the Home page illustrations. Six rulings, worked through in order, each reported before building and built only after sign-off.
  **1. POLICY — the Master Plan's AI-illustration exception is extended to baked-in text on body images.** Neither the exception itself (silent on text) nor the Featured-Image Standard's text-overlay exception (scoped to the `og:image` artifact for Buyers/Sellers articles only) nor the "everywhere else" clean-photo rule (about page titles, a different kind of text) actually governed this case — reported as a real gap rather than assumed covered by the Home precedent. Bundled version bump: Master Plan v2.22 → **v2.23** (`docs/approved-copy/V2_23_Website_Master_Plan_Lisa_Collio.md`, v2.22 deleted), CLAUDE.md → **v2.7**, both in this change. New rule: alt text for a text-bearing body image must convey the baked-in message, not describe the illustration generically.
  **2. FOLDERS — moved out of the flat upload into the structure Lisa specified,** `assets/images/buyers/`, `process/`, `why-work-with-lisa-cards/{en,es}/`, via `git mv` (history preserved). Five filename mismatches from the original upload corrected in the same move: `figure-out-what-you-looking-for` → `-are-looking-for`, `when-you-find-the-right-one-move` → `when-you-find-the-right-one`, `your-offer-gets-thought` → `-thought-through`, and the two trailing-space files renamed per ruling 6.
  **3. FILE COUNT — 17 confirmed, 17 accounted for, 0 missing, 5 misnamed** (folded into item 2's move). Reconciliation was by matching each intended concept against the actual branch content, not by count alone.
  **4. THE 7TH PROCESS STEP — built after Lisa approved the copy in both languages.** New step 1, "Consult with Lisa First" / "Primero, hable con Lisa," native Spanish (not translated — drafted independently, matching the existing steps' voice), existing 6 steps renumbered 2–7 in both `buyers/index.html` and `es/compradores/index.html`, text otherwise byte-for-byte unchanged from what was reported. CSS gained `.info-card__img`/`.process-step__img` (unforced aspect ratio, no crop — several illustrations carry baked-in text near the edges that a forced `object-fit` crop could cut off), replacing a design comment that had called the process-step cards "deliberately... no imagery"; updated in place rather than left contradicting the new markup.
  **5. THE TWO REAL PHOTOS.** `buying-a-home.jpg` was a confirmed byte-identical duplicate of `assets/images/client-reviews/review-maria-baltazar.jpg` (SHA-256 match) — deleted from this branch rather than duplicated. `why-work-with-lisa.jpg` matched nothing by hash or dimension anywhere in the repo; per Lisa's ruling, filed as new: renamed to `assets/images/client-general/clients-best-realtor-ever-title-office.jpg` (following that folder's naming convention), full iPhone EXIF present (no provenance concern), consent recorded in `licensed-assets.txt` per Lisa's confirmation that she holds written permission from both clients pictured — not paired with a review, so no `lisa-collio-google-reviews.md` mapping row applies; this is its consent record. Not wired onto any page this round — filed and catalogued only, since no page slot was reported or approved for it.
  **6. TRAILING-SPACE FILENAMES — both renamed** (`she-working-for-you .png` → `she-works-for-you.png`, `lisa-conoce-este-mercado .png` → `lisa-conoce-este-mercado.png`), folded into item 2's move. Confirmed by re-checking all 17 basenames for any whitespace: these two were the only ones.
  **A bilingual-purity gap found and deliberately not silently built around, CLOSED 18 August 2026.** `get-pre-approved-first.png` carried English-only baked-in text ("BANK," "PRE-APPROVAL"). Wiring it onto `/es/compradores/` would have put English text on a Spanish page, in tension with the locked "no mixed-language pages, ever" rule — a conflict ruling 1's policy approval didn't specifically address, since it was about whether baked-in text is allowed at all, not about cross-language reuse of an English-text image. Wired on the English page only at the time; the Spanish page's matching step (02, "Primero, la pre-aprobación") shipped with no image rather than an English-text one, recorded as an open item pending Lisa's call.
  **Resolution:** Lisa generated `get-pre-approved-first-es.png` (broker approval same as the other 15 illustrations, no new approval needed), matching composition and style, baked-in text "PRE-APROBACIÓN"/"BANCO" — verified by direct inspection to contain no English text before wiring. Dimensions 1231×1278 (slightly portrait, not landscape as first assumed) — clears the 1200px floor, unlike the other 15 illustrations (1080×1080, still under-floor). Different aspect ratio than the English file (1080×1080 square); not cropped to match, since `.process-step__img` has no forced aspect ratio. Wired into step 02 on `/es/compradores/` only — the English page and its image untouched. `licensed-assets.txt` entry added, same format as the other 15. **Both language pages now carry an image on every process step.**
  **`content/source/licensed-assets.txt` gains 16 entries** — one per AI-generated file (15: 7 process + 4 EN cards + 4 ES cards) plus the one real-photo consent record above, following the exact field format of the existing Home-page entries. All 15 AI files are 1080×1080, under the Hero Standard's 1200px floor — same precedent as `home/real-estate-guidance.jpg`, logged as a post-launch replacement candidate rather than a build blocker, not re-litigated per-file.
  **WEBP siblings generated for all 16 new files** (Pillow, quality 82) and wired via `<picture><source type="image/webp">` for all 21 body-image insertions — the closest existing pattern for a body-content image (`.section-figure` on the Home page) uses `<picture>`/webp; the flatter `.local-card__img` (blog topic cards) does not, and card-image markup here follows the section-figure precedent since these are full illustrations, not small card thumbnails. PNGs also re-saved optimized (Pillow, `optimize=True`) — roughly halved total size (22.2MB → 12.4MB) as a fallback for browsers without WEBP support, not the primary delivery format.
  **Reconciliation:** `content/image-parity-baseline.json`'s `/buyers/` ↔ `/es/compradores/` pair re-measured fresh against the actually-applied pages — **27/25** (was 16/15), the 2-image gap consisting of the 1 pre-existing gap plus the 1 new deliberate gap from `get-pre-approved-first.png` not shipping on the ES page. `build-reviews.js` reshuffled 130/136 pages site-wide (expected — the new client-general photo brought the review-photo pool from 28 to 29; spot-checked, confirmed as ordinary picture/blockquote swaps, not corruption).
  Full pipeline clean: `build.js --check`, `hash-assets.js`, `build-reviews.js --check`, `npm run llms -- --check`, `npm run audit` (one pre-existing, unrelated SEO warning).
- [ ] **`review-maria-baltazar.jpg` carries two different English alt strings across the seven pages where it already appears — needs a canonical-string decision.** Found 18 August 2026 while reusing this file's alt text for a new Buyers-page placement, per the Alt-Text Correspondence Standard. Six pages use the generic `"Client of Lisa Collio in Goshen and Elkhart, Indiana"` (small `review-card__photo` review-grid context); the homepage alone uses the more specific `"Maria Baltazar celebrating her new home"` (full-size, uncropped usage). Pre-existing, not introduced by the Buyers-page work — flagged, not fixed, per Lisa's explicit instruction not to resolve it inside that PR. Needs a decision on which string is canonical (or whether the split is intentional — small-thumbnail context vs. full-image context — and should stay as two deliberate strings rather than one).
- [ ] **PR #123** — Master Plan v2.21 bundled bump (v2.20 deleted): records the hero programme for the first time — Hero Standard as a governing document, the Image Review Policy narrowing, the corrected cutout scope (Home EN/ES only), the eighteen-versions-stale Market Stats ES-pair correction, and two new §19 registry rows. Caught and corrected a seventh instance of the changelog self-miscounting pattern in v2.21's own drafting before it shipped (Lisa's count, not this session's — Claude Code found it, Lisa supplied the corrected file). CLAUDE.md bumped to v2.5 to match (cutout-scoping claim corrected; Image Review Policy needed no CLAUDE.md content change since it was never encoded there). Closes the Maple City emblem item below under the narrowed policy. **Awaiting merge.**
- [x] **PR #82** — blog verify-current-details disclaimer component. **Merged 29 July.**
- [x] **PR #83** — Spanish FAQ images + EN/ES parity check, with all requested alt-text revisions. **Merged 29 July.**
- [x] **PR #84** — homepage coded-descriptor fixes, both languages. **Merged 29 July.**
- [x] **PR #86** — article rename + "quiet street" fix. **Merged 29 July.**
- [x] **PR #87** — description identity fixes + `audit.js` check 18. **Merged 29 July.**
- [x] **PR #88** — punch-list maintenance: closed the alt-text/description-sweep session's items, added the og/twitter-fields-absent item, the (then-41, later corrected to 32) over-length-descriptions item, and scoped the phone-number convention without applying it yet. **Merged 29 July.**
- [x] **PR #89** — meta/og descriptions brought into the 145–160 band (51 rewrites) and the phone-number convention settled (EN/ES forms, high-intent pages only, office-address pages on `/contact/` and `/es/contacto/` held). Audit SEO warnings 41 → 9. **Merged 30 July.**
- [x] **PR #90** — folded the still-true parts of `docs/GOSHEN_SECTION_IMAGE_NOTES.md` (emblem geometry, the empty-commit incident) into §A and retired the doc unmoved rather than moving it verbatim — two of its three sections were stale or actively contradicted production. **Merged 30 July.**
- [x] **PR #91** — title-length fixes: an entity-decoding bug in `audit.js` meant 6 of the 9 "over-60" titles were never actually over (`&amp;` was scoring 5 characters instead of 1); the 3 genuinely over-length titles were rewritten and 3 buyers titles gained the `| Lisa Collio` suffix. RV-industry article H1 aligned to its shortened title. `OVERLAY_JOBS` backfilled for all 37 Template A slugs. Audit SEO warnings 9 → 0. **Merged 30 July.**
- [x] **PR #92** — committed the Master Plan to the repo for the first time, as v2.13 (see the new §G item on why this mattered). **Merged 30 July.**
- [x] **PR #93** — CLAUDE.md regenerated to v1.6, folding in all eight Master Plan §8 standards — the three from v2.12 that had never been mirrored into CLAUDE.md at all, plus the five added/amended in v2.13/v2.14. Repo Master Plan copy replaced with v2.14, superseding v2.13. **Merged 30 July.**
- [x] **PR #98** — copy-field-gap sweep: inventoried which copy-field classes (schema fields outside description, image `title` attrs, figcaptions, CTA text, form labels/placeholders, `aria-label`) had no compliance rule at all. Added `audit.js` checks 19 (locked identity rules in `<title>`, catching 3 live "Realtor" violations check 18 never scanned for), 20 (schema `headline` ↔ `<h1>` sync), and 21 (sold-listing address claims must be in `content/source/sold-listing-consent.txt`, 876 IAC 8-1-8(f)). Also closed the `/es/vendedores/` gap for 501 Danbury Dr (see §D). **Merged 31 July.**
  **Folded in from the branch copy of v2.8, 5 August.** This entry existed only on `claude/compliance-gaps-copy-fields-scwmod` and never reached `main`'s punch list — which meant checks 19, 20 and 21 were described nowhere in the record while running on every build. See the v2.9 header line.
- [ ] Delete each branch on merge. GitHub UI only — the Claude Code git proxy rejects ref deletions. **`claude/goshen-images-audit-8y60q1` is deliberately kept**: it holds `docs/GOSHEN_SECTION_IMAGE_NOTES.md`, which never reached `main`.
  - [ ] **`claude/compliance-gaps-copy-fields-scwmod` — RETIRE (Lisa, 5 August 2026).** Its PR (#98) merged 31 July, but a later commit (`c45dbee`) added PR #98's follow-ups to that branch's copy of the punch list and was never merged. Everything unique on it is folded into v2.9 above; **verified item-by-item against `main`'s copy before retiring, not assumed** — three items folded in, two dropped as superseded. Nothing further on it is unique. Safe to delete in the GitHub UI.
    **The lesson, for the Master Plan.** This is the Single Open-Items Record Standard breaking in the way the standard does not currently describe. It bars a *parallel* record, and nobody created one deliberately — the punch list was edited on a feature branch, that branch's PR merged, the later commit did not, and `main` moved on. Two files with the same name and the same version number then diverged for five days with no diff, no warning, and no way to tell which was current from the filename. **The standard needs a mechanical corollary: the punch list is edited only on a branch that is going to merge, and any branch whose PR has merged is checked for unmerged commits touching it before being retired.** A version number in the filename does not protect against this — both files said v2.8.
- [x] **PR #114 — Master Plan v2.19 bundled bump, single-item correction.** Fixes §19 row 1's Exact filename cell (still `V2_16_...`, four versions stale, beneath a note claiming the row named none) — found in chat while reading v2.18 ahead of its bundled bump, confirmed by Claude Code against the merged file. v2.19 committed to `docs/approved-copy/`, v2.18 deleted in the same change, CLAUDE.md regenerated to v2.2 with attribution matching v2.19's §20 entry exactly (Claude found it in chat, Claude Code confirmed it against the merged file — not the reverse). Same PR also corrects this punch list's own two "extinct by construction" occurrences (STANDING NOTE above), which carried the identical over-claim v2.19 fixes in the Master Plan, and adds `audit.js` check 24 guarding §19 row 1's Exact filename cell specifically — proven against v2.18 (flags) and v2.19 (clean) before shipping. **Merged.**
- [x] **PR #115 — aria-label language fix + `audit.js` check 25, 11 August 2026. Merged.** `build-reviews.js`'s `stars()` helper hard-coded `aria-label="5 de 5 estrellas"` regardless of page language; fixed to take `lang` and emit `"5 out of 5 stars"` on EN pages, `"5 de 5 estrellas"` on ES. **Shipped as a direct edit to the 74 live EN pages (148 substitutions, one string, nothing else touched), not a `build-reviews.js` write run** — a write run would have simultaneously applied the current rotation assignment to the 59 of those 74 pages that are also in the (separately tracked, re-measured to 109) stale-rotation set, restating reviewer content nobody asked to restate. Proven: `git diff` shows only the aria-label string changed on all 74 files, all declaring `<html lang="en">`; `build-reviews.js --check` before and after shows the identical 109-page stale set, unchanged by this fix. Also closes the false §D `/es/vendedores/` claim (traced to its root cause, see §D) and logs two new §D items (13-vs-11 sold-listings showcase; `.visually-hidden` "Artículos relacionados" EN/ES gap) plus the 53→109 rotation-count correction. Did **not** rename this file to v2.10 or touch Master Plan §19's punch-list/Audit-Checklist citations — both waited for Master Plan v2.20 (next entry).
- [x] **PR #116 — Master Plan v2.20 bundled bump, single correction, 11 August 2026.** Completes the sweep v2.19 started: §19's "Governing documents" table rows 2 (Pre-Launch Punch List, named `V2.9 Pre Launch Punch List Lisa Collio.md`) and 3 (Audit Checklist, named `V1_4_Audit_Checklist_Lisa_Collio.md`) carried the identical hardcoded-version defect v2.19 fixed in row 1 — the sixth instance of the Field-Class Compliance Coverage Standard, second in consecutive versions. v2.20 committed to `docs/approved-copy/`, v2.19 deleted in the same change; CLAUDE.md regenerated to v2.3, including genericizing its own line 6 ("Bundled-bump rule (Audit Checklist v1.3)" → "...(Audit Checklist)") — the identical defect, one document over, caught by the same sweep before it shipped a seventh instance.
  **Two live pointers outside §19 fixed in the same PR, found during the pre-v2.20 sweep:** `audit.js`'s header comment cited `V1_4_Audit_Checklist_Lisa_Collio.md` by exact name — genericized to "the current Audit Checklist... always the highest-numbered." `scripts/generate-featured-images.py:280` cited `docs/V2.3 Pre Launch Punch List Lisa Collio.md, section E` — already broken before this PR (that file was deleted long ago; current is this file, and the item it points to has since moved to §C, not §E). Rewritten to name the current file generically and to search by content (`costos-de-cierre-que-son`) rather than a section letter, since the letter had already drifted once.
  **`audit.js` check 24 widened**, scoped to the whole 4-row "Governing documents" table (not just row 1), pattern generalized from `V2_[0-9]`/`v2\.[0-9]` to `\bV\d[_.]\d` so it also catches the Audit Checklist's `V1_4` form. Proven against v2.19-on-`main` (a real known-bad case, not planted): **exactly 2 errors**, rows 2 and 3 by name. Confirmed 0 false positives against the "Website documents"/"Added in v2.16" tables, which carry 24 legitimate version strings the check must never flag. Confirmed 0 against v2.20.
  **All six one-time Claude Code instruction files whose batch has merged were deleted, not just the one first flagged** — the Master Plan's own §19 ruling names this class (delete once the batch merges), and deleting one while leaving siblings would have been the same field-class error this whole bump documents. Each verified individually, against git/the live repo, not inferred from its filename:
  | File | Batch | Verified merged, how |
  | --- | --- | --- |
  | `docs/drafts/..._Elkhart_Images_...md` | Elkhart section/FAQ images | 108 Elkhart section image files live in `assets/images/sections/`; PRs #108–#112 merged to `main` |
  | `docs/drafts/..._Text_Overlay_Exception_...md` | Featured-image text-overlay reversal | `OVERLAY_SCOPE` mechanism (PR #71, merged 27 July) still live and actively maintained in `scripts/generate-featured-images.py` on `main` today. **Caveat, recorded rather than dropped: `npm run check:images` could not be re-run to re-confirm the previously-recorded 37/58 clean split — the environment is missing the Python `PIL` module. Merge status was established a different way (above), not by reproducing that specific measurement.** |
  | `docs/approved-copy/..._Batch_2b_Review_Rotation_...md` | Full review rotation | `build-reviews.js` and `content/reviews.json` live, actively maintained (edited in PR #115, immediately prior) |
  | `docs/approved-copy/..._Batch_2c_Legal_Pages_...md` | Spanish legal pages | `es/privacidad/`, `es/terminos/`, `es/accesibilidad/`, `es/vivienda-justa/` all live on `main` |
  | `docs/approved-copy/..._Batch_2e_Pages_...md` | Spanish Goshen pages | `es/viviendo-en-goshen/`, `es/mudarse-a-goshen/` live on `main` |
  | `docs/approved-copy/..._Batch_2e_Articles_...md` | Six Spanish Goshen articles | Spanish Goshen articles live under `blog/spanish/` on `main` |

  Zero other files reference the four Batch-2b/2c/2e filenames anywhere in the repo (clean, isolated deletions). The Elkhart Images and Text Overlay Exception filenames are each referenced by dated, one-time Claude Code session reports (`docs/ELKHART_IMAGE_*.md`, `docs/FEATURED_IMAGE_OVERLAY_AUDIT.md`) and CLAUDE.md's own dated Revision Log — all historical data per the Changelog Citation Standard, correctly left unedited after deletion.
  **Closes two §F "verify this landed" boxes as a side effect** — Batches 2b and 2c, the first direct confirmation either actually happened; see §F.
  **Filename/version stay at V2.9 this bump too** — the rename to v2.10 is now unblocked (Master Plan §19 no longer hardcodes a punch-list version to strand), but wasn't part of this PR's scope; still pending.

## C. Decisions still open

- [x] **Seventh instance of a claim verified once and carried forward instead of re-derived — CLOSED 13 August 2026, same session it happened in.** During the PR #118 build, a `git stash` test correctly showed the `image-parity` check clean at that point mid-build — before the Buyers `.hero__portrait` removal had happened yet. The conclusion ("pre-existing, confirmed via git stash") was then written into the final PR report **without re-running the check against the finished diff**, by which point the portrait removal had actually introduced a real gap (Buyers `<img>` count 17→16, `es/compradores/` unchanged at 15 — see the item below). Lisa asked directly whether PR #118 caused the error; re-checking against a clean worktree of the actual merge commit confirmed it did.
  **The mechanical lesson, stated so it doesn't need re-deriving next time: a verification run mid-build is not evidence about the final diff.** Any check run before the last commit of a branch must be re-run against the branch's actual final state before that result is used in a report — not carried forward from whenever it was convenient to run it. This is the same defect class the Verification Standard and its "state a limit once, apply it to the whole report" corollary already cover, applied here to *when* a check runs rather than *what* it covers.
  **A second, independent limitation surfaced by the same sweep, recorded because it's real even though nothing broke from it:** `audit.js` check 14 only compares EN's image count against ES's. Five of the twelve hero-touched page pairs (Living in Elkhart, Living in Goshen, Moving to Elkhart, Moving to Goshen, Next Chapter Method) lost one image from their old hero on **both** languages symmetrically, and check 14 stayed silent on all five — correctly, since parity held, but silently by design, not because it looked and confirmed nothing changed. **The check cannot see a symmetric change at all — it has no way to.** "The check stayed green" is not the same claim as "nothing changed," and this project has a build-record standard for exactly this reason on the image-generation side; check 14 doesn't have an equivalent on the page-content side. Not a defect to fix now — recorded so the next session doesn't mistake check 14's silence for a broader guarantee than it makes.

- [x] **Master Plan v2.16 — corrections — ALL EIGHT CLOSED IN v2.17, 5 August 2026.** Every item below was reported against v2.16 and is now fixed at the source. v2.17 is committed to `docs/approved-copy/V2_17_Website_Master_Plan_Lisa_Collio.md`, v2.16 deleted in the same change, CLAUDE.md regenerated to v2.0. **Verified item by item against the committed v2.17, not assumed from its changelog.** Kept in full rather than deleted — three of the five original findings were withdrawn, and the withdrawal is the part worth keeping.
  **Three of the five originally reported were withdrawn on 5 August after Lisa supplied the missing evidence.** Kept here in full, because the retraction is the more useful record — see the new standing rule directly below this item.
  1. ~~**Review-Surface Standard cites an incident with no record.**~~ **WITHDRAWN — the incident is real and is the standard's origin.** In the PR #105 review Lisa said the Elkhart High School sign was not legible at delivered size; the correction was that the lettering occupies roughly **370×80 px in the 1200×630 delivered file and reads ELKHART HIGH SCHOOL clearly** — what was illegible was the contact sheet, where each tile is 372 px wide, about a third of delivered size. **That exchange is where the 3× figure in the standard comes from.** My check found no record because the exchange lives in chat, not in a committed artifact. Keep the example as written.
  2. ~~**Report-Before-Build cites a slug collision.**~~ **WITHDRAWN — the collision is real.** A drafted Spanish downtown-Elkhart article was blocked because `/blog/spanish/centro-de-elkhart-indiana/` was already published; building "a new article" at that path would have silently overwritten live, already-approved, already-linked content. Also in chat, also absent from the repo. Keep it. *(The `CLEAN_JOBS` repoint trap I offered as the "real" finding is a separate, genuine catch — it is what the Source-Repoint Standard covers, not a substitute for this one.)*
  3. **Report-Before-Build's §2-vs-§8 citation needs its attribution amended, not removed.** The correction is real; it was **PR #102, on `docs/compliance/Federal-Fair-Housing-Act-Website-Compliance-Reference.md`** — its line 7 records it: the EHO checklist item cited "Master Plan §2/§18," §2 (Identity Block) holds no Equal Housing content, the locked footer stack's EHO line lives in §8, corrected to §8/§18. The standard implies it was found in the Elkhart instructions file, which cites §8 correctly. **This one was in the repo the whole time**; I grepped only the Elkhart instructions file, having assumed the claim was about it — the wrong artifact tested, and the null result reported as a finding.
  4. **The §19 Reference Data Sheet row corrects one false claim and leaves two standing.** It rightly says the file contains no banned-term list, then asserts "the 'walkable' removal, ZIP-code guidance and illustrative-commute-figure flag are confirmed present." Measured: `walkab` → **0 hits** (confirmed). ZIP-code guidance → **absent**, no `46526`/`46528`/ZIP text in the file. Commute flag → **absent**; the file cites "the median commute is about 19 minutes" as an example of a fine factual use, not as illustrative. The row attributes the confirmation to the §19 audit, which never checked those two.
  5. **The same corrected claim survives in §15 and §8.** §15 still reads "now v1.1, both instances removed, full banned-term list added"; §8's page-standards line still cites "Goshen–Elkhart Reference Data Sheet **v1.1**" — the version §19 just ruled does not exist. **This is the Field-Class Compliance Coverage Standard applied to the Master Plan itself:** the fix landed where the problem was visible and the parallel statements carrying the same content were not checked. §20's v2.6 changelog repeats it too, but that one is data and must stay.
  - **Also, live pointers gone stale inside v2.16, none load-bearing:** §9 and §17 still call the reviews document "v2" while §19 correctly drops the version and the file itself reads **v3.2**; §14 cites `Lisa_Collio_Website_Build_Plan_v1_0.md` while §19 registers `v1_2`.
  **The v2.17 worklist — all eight closed, each verified in the committed document:**
  | # | Item | Closed in v2.17 |
  | --- | --- | --- |
  | 1 | Amend #3's attribution to PR #102 and the Fair Housing reference | ✅ §8 Report-Before-Build now names the Fair Housing compliance reference and PR #102, with the §2/§18 → §8/§18 detail. Incident stands; only the attribution moved. |
  | 2 | Fix the §19 Reference Data Sheet row (#4) | ✅ Row now states only the "walkable" removal as confirmed, and records that v2.16's ZIP-guidance and commute-flag assertions were measured absent. |
  | 3 | Sweep #5's parallel statements in §15 and §8 | ✅ §15's "now v1.1 / full banned-term list" claim corrected and redirected to `content/source/fair-housing-terms.txt`; §8's page-standards line no longer cites a v1.1. §20's changelog repetitions left intact — a changelog is data. |
  | 4 | Update the reviews "v2" pointers in §9 and §17 | ✅ **And a third the worklist missed** — §8's Images section also said "reviews doc v2". Measured: three pointers in v2.16, zero in v2.17. |
  | 5 | Update the Build Plan version in §14 | ✅ Now version-agnostic. |
  | 6 | Remove the `FEATURED_IMAGE_OVERLAY_AUDIT.md` flag | ✅ Recorded as resolved mechanically, audit right and CLAUDE.md wrong. |
  | 7 | Remove §19 row 27 | ✅ Row gone from the table; the reason recorded in §19 rather than the row silently vanishing. |
  | 8 | "Six approved Goshen source documents" → five | ✅ Corrected in §19 and the standards text. |

  **Leave #1 and #2 exactly as written** — done; both withdrawn findings are untouched in v2.17.
  **CLOSED IN v2.18, 6 August 2026: v2.17's changelog undercount corrected.** It said the reviews-version pointer was corrected in "§9 and §17." It was corrected in **three** places — §8's Images block said "reviews doc v2" as well. Measured: 3 pointers in v2.16, **0** in v2.17 (the fix was complete; only the description was short). v2.18's own §20 entry states this exactly, including the 3-vs-0 measurement. **Verified before closing:** counted v2.18's changelog claim per the mechanical rule below — it claims two items changed, and the committed document's §20 v2.18 entry carries exactly two (the Review Language Standard, and this correction). No new undercount shipped in the version that fixed the last one.

- [x] **STANDING NOTE — the Master Plan's self-reference bug is a known property of the document, not a run of accidents (Lisa, 5 August 2026). CLOSED IN v2.18, 6 August 2026; correction applied and re-closed in v2.19, 11 August 2026.**
  > **Fifth instance.** Every version of this document that describes its own changes has, at least once, described them wrongly. The bump that amends v2.17's changelog should say so — recording the pattern rather than the fifth data point, because five instances across nine versions is a property to design a check around, not a streak to keep noting.

  **v2.18's §20 entry does this:** it names the fix as the fifth instance of the document misdescribing its own changes (v2.8, v2.11, v2.14, v2.16, v2.17), splits the family into stale-self-citations and undercounts (a different failure, checkable only by counting), and states the mechanical check going forward: **count a changelog's claimed items and diff against the prior version before the bump ships.** Both known undercounts (v2.14, v2.17) were in fact caught exactly that way, after shipping rather than before — recorded in the table below for reference.

  **Corrected in v2.19: the stale-self-citation sub-form was declared "extinct by construction" here and in v2.18's own §20 entry, on the strength of §19 row 1's Role column having been cleared. It wasn't — the row's Exact filename column still read `V2_16_…` at the time both of those sentences were written, and stayed that way through v2.17 and v2.18 before v2.19 caught it.** A claim of "extinct by construction" that covers one of two fields in the same row is not construction; it's the same Field-Class Compliance Coverage failure the rest of this entry describes, just not yet applied to this document's own metadata. v2.19 fixes §19 row 1 itself (both the Role column and the Exact filename column now name no version) and is the sixth instance, not a footnote to the fifth.

  The six, with what each actually was — **two distinct sub-forms, and the check differs for each**:
  | Version | Instance | Form |
  | --- | --- | --- |
  | v2.8 | The v2.7 edit bumped the banner and changelog but never cascaded into §19; the Master Plan's own registry row still said v2.6 | **Stale self-citation** |
  | v2.11 | Two registry rows missed because the work landed on a parallel branch | **Stale self-citation** (missed cascade) |
  | v2.14 | v2.13's changelog said "four new locked standards"; the body carried five | **Undercount** |
  | v2.16 | Row 1 read "this doc, v2.11" inside a v2.15 document — stale through four versions | **Stale self-citation** |
  | v2.17 | Changelog says the reviews pointer was fixed in "§9 and §17"; it was three places | **Undercount** |
  | v2.19 | §19 row 1's Exact filename cell still named `V2_16_…`, four versions stale, beneath a note asserting the row named none | **Stale self-citation** (sibling field, not swept) |

  **Why the distinction matters rather than flattening them:** a stale self-citation is caught by *removing the number* — which §19 row 1 now does in **both** its cells as of v2.19, naming no version at all, and which this project has applied to CLAUDE.md's header, the punch list's governing-documents line, and the Audit Checklist. Whether that form is now actually extinct by construction, rather than merely declared so again, is exactly the claim v2.19 exists to leave unasserted a second time — the mechanical check below is what would catch a seventh instance, not the declaration itself. An **undercount** is a different failure and none of those fixes touch it: it is a claim about how many things changed, checkable only by counting the body against the summary. **Two of six are undercounts and both were found by measuring, not by reading; two of six are the same "extinct by construction" over-claim, one in the Master Plan and one right here in this punch-list item, both corrected in the same PR that fixed the Master Plan's.** The cheap mechanical version, if one is ever wanted: before a bump ships, count the items its changelog claims and diff that against the previous version — and for a self-citation fix, search the full row or field, not just the one string already known to be wrong. Both undercounts would have been caught that way, and both were in fact caught exactly that way — v2.14 by regenerating CLAUDE.md, v2.17 by diffing v2.16 against it in full.
  **Two more for v2.17, added 5 August:**
  - **§19 row 27 points at a file that exists nowhere — remove it (Lisa, 5 August 2026).** `Goshen-Cluster-Decisions-and-Photo-List.md` is not in the repo and Lisa confirms it is not in the Project either. **Ruling: remove the row and record that the file could not be located**, rather than leaving a citation a future session cannot resolve. A registry row pointing at nothing is worse than an absent row — it reads as a file someone merely failed to find, and invites the search to be repeated indefinitely.
  - **Wording fix — "six approved Goshen source documents" is wrong** and both §19 and the standards text carry it. Five are approved page/article sources; the sixth (row 27) is a working reference the registry itself describes as "not build content." The phrasing originated in Claude Code's §19 audit and was drafted into v2.16 from it.

- [x] **STANDING RULE — a repo-scoped review cannot verify a chat incident, and absence from the repo is not evidence it did not happen. For the Master Plan (Lisa, 5 August 2026).**
  > A review with access only to committed artifacts can establish that something **is not in the repo**. It cannot establish that it **did not occur**. Where a claim describes an exchange, a decision, or a correction that happened in chat, say *"no record in the repo, which does not settle it"* and ask — never report it as unfounded.

  Earned immediately, three times in one pass. Reviewing Master Plan v2.16 against the built record, I reported three incidents as misstatements on the strength of a grep returning nothing: the Elkhart High School sign legibility exchange (the origin of the Review-Surface Standard's own 3× figure), the `/blog/spanish/centro-de-elkhart-indiana/` slug collision that would have overwritten a live published article, and the §2-vs-§8 citation correction. All three were real. Two happened in chat and were never committed; **the third was in the repo and I tested the wrong file for it** — a scoping error inside the check, not a limit of the repo.
  **What makes this worth a standard rather than a note:** the correct framing was already in the report. Finding #3 stated the limit explicitly — "only committed artifacts can be checked from here" — and then applied it to that finding alone, while #1 and #2 were reported flatly as having no record. Stating a limit once does not discharge it; it applies to every finding of the same class in the same report. This is the Verification Standard's "state what was actually measured" failing at the point where it matters most, because a confident retraction of a true incident deletes the evidence a standard rests on. The Review-Surface Standard would have lost its own origin.

- [ ] **~111 MB of orphaned originals** in `assets/images/goshen/`. Nothing references them; the live images are the processed copies in `assets/images/sections/` (14.4 MB). Keep in-tree or remove and rely on git history? The same question was already answered "remove" for the processed set.
- [ ] **Six legacy Elkhart landmark filenames** (`downtown-lerner-theatre.jpg`, `island-park.jpg`, `wellfield-botanic-gardens.jpg`, `riverwalk.jpg`, `ruthmere-museum.jpg`, `neighborhood-street-elkhart.jpg`) referenced across 4 doc lines in two approved-copy files. Retire the block as the manifest proposes, or repoint. **Blocked until the Elkhart photos land.**
- [ ] **Empty/incomplete sold-property folders:** `501-danbury-dr-goshen-in-46526/` (README, no photos) and `58832-saint-marys-ln-goshen-in-46528/` (empty).
  **501 Danbury Dr — deliberately empty, not an oversight (Lisa, 2 August 2026).** Lisa has a photo for this address and is not using it: the photo's provenance isn't clear enough. The folder stays empty and this item stays open. Do not fill it. Written consent for the *address* is on file (see the consent manifest note below) — the block is the photo, not the consent. This is the pattern the Rejected-Asset Standard exists for: an empty slot with a reason reads identically to an oversight unless the reason is written down.
  **`docs/FEATURED_IMAGE_OVERLAY_AUDIT.md` vs CLAUDE.md — RESOLVED 5 August 2026, in favour of the audit.** v2.16 flagged the two as disagreeing and left it open. Settled mechanically: `npm run check:images` reports 37 in-scope slugs carrying a band, 58 out-of-scope carrying none, "every variant matches its scope" — zero out-of-scope images with baked-in text remain, which is what the audit says and what CLAUDE.md denied. CLAUDE.md's live line corrected in v1.9; its v1.5 changelog entry keeps the 49-slug figure, which was true when written.

  **Sold-listing consent for the address is confirmed** — Lisa, 2 August 2026, all 14 addresses. Canonical record of that item and its history is in §E; not repeated here, so there is one place to correct if it ever changes.

- [ ] **4 featured images still carry a title older than the article's current H1 — verified 30 July, still not regenerated.** `costos-de-cierre-que-son`, `no-se-si-califico-credito-y-preaprobacion`, `prestamos-fha-y-usda-en-espanol`, `cuanto-vale-mi-casa-elkhart` — all Spanish authority articles. Found during the `OVERLAY_JOBS` backfill (see the Mechanical checks section). The image and the article now say different things at a glance.
  **Confirmed directly against the repo, not assumed:** `scripts/generate-featured-images.py`'s `OVERLAY_JOBS` still records the old baked title for all four (each row is commented "baked title predates live H1"), and `git log` on all four `-header.jpg` files shows their last commit is PR #19 (22 July) — before the H1 revisions that created the drift. Nothing has regenerated them.
  **Not the same item as the RV-industry article, and the two should not be conflated.** That article's featured image was never in `OVERLAY_SCOPE` to begin with — `working-in-the-rv-industry-and-buying-a-home-in-elkhart-indiana` and its Spanish twin are both `CLEAN_JOBS` entries (clean photos, no baked text). Its H1 realignment in §E below was a text-only change; no image ever needed regenerating, and none was.
  Still open: regenerate the four Spanish images to match their current H1, or confirm the shorter baked phrasing was a deliberate design choice and leave it — either is a two-minute job now that the source photo and variant are recorded.

- [x] **The five approved Goshen source documents are committed — 5 August 2026.** They had driven `/living-in-goshen/`, `/moving-to-goshen/`, six authority articles, `/disclaimers/` and `/sources/` while existing only as Project uploads, and repo files cited two of them by name as their factual basis. **Build Procedure rule 3 closed for the Goshen cluster.** Each was diffed against the pages actually built from it *before* committing, and divergence reported rather than resolved silently: **439 sentences compared across the five, 8 divergent, all in one paragraph pair.** Four defects repaired on the way in, each recorded in a dated as-built note inside its own file — a spliced and duplicated block with a broken `═══ PAGE CONTENT ═══` marker, ten retail business names predating the retail/dining ruling, a Compliance Fix Log describing a page state three rewrites out of date, and a spoke list whose framing contradicted its own heading. **All five now diff to zero against their live pages.**
  **Row 27 is not among them** — see §C. Five files, not six.

- [ ] **`audit.js` check 28 (hero contrast) — decide what it should actually be.** Raised directly by Lisa, 13 August 2026: the check was reported as "a mechanical WCAG AA sampler" when what it actually does, under the current CSS, is guard two infrastructure conditions (broken `--hero-img` reference; missing sampler script/dependency) — the contrast-threshold branch cannot fail for any photograph, verified against the true worst case inside the text zone (66% scrim opacity at the gradient's 52% boundary), not just this check's own sampling choice. That's a structural guarantee of the current scrim opacity and text-column width, not something this check adds. **Three options, undecided:** (1) keep it as-is but rename/redocument it honestly as infrastructure-only, dropping any contrast-coverage claim; (2) redesign it to guard the CSS invariant itself — flag if `.hero--photo`'s scrim opacity or `.hero--tier2 .wrap`'s `max-width` ever change, since either is the one way a future photo could actually fail; (3) remove the dead threshold branch entirely. Until this is decided, real hero photos must be contrast-checked by eye as they land — see the note in §A's twelve-hero-photographs item.
- [x] **`audit.js` check 26 (hero placeholder) — FLIPPED WARNING to ERROR, 13 August 2026.** The condition this item recorded — "once every one of the twelve hero photos above has actually landed" — is met (see the item above). Flipped in the same PR that wired the real photos, not before. **Proven, not just changed:** a mutation test (temporarily reverting one page to the placeholder filename + attribute) confirmed the check now fails the run (`exit 1`, reported under ERRORS not WARNINGS), then reverted clean (`exit 0`).
- [x] **Home's hero build-record gap — CLOSED 13 August 2026.** Flagged in this project's Phase 0 report ahead of the Hero Standard build: Home's hero background (`hero-twilight-1.jpg` / `hero-twilight-2.jpg`) had no build record anywhere — no entry in `SECTION_JOBS`, `CLEAN_JOBS`, or `OVERLAY_JOBS`, and no script that could reproduce how those two files were made. Per Lisa's ruling, Home now carries the same shared placeholder as the eleven Tier 2 pages (`data-hero-placeholder="true"` on both `/` and `/es/`) rather than keeping the unrecorded twilight photos, and is recorded as slot `"home"` in the new `HERO_JOBS` table (`scripts/process-hero-images.py`, renamed from `generate-hero-placeholder.py` later the same day when the real photos landed) with its source, crop behavior and target resolution. The gap closes for the placeholder; it reopens narrowly the moment Home's real hero photo lands, at which point that slot's `HERO_JOBS` entry needs its own source + blob SHA, same as the other eleven.
- [x] **Mobile nav open panel unreachable below the fold — CLOSED 13 August 2026. Live since PR #71, 27 July 2026 — roughly 2.5 weeks, not caught by a full site audit or any of the 27 mechanical checks that existed at the time.** Found via Lisa's own phone, reported as "the deploy preview does not scroll," which turned out on investigation to be the expanded mobile menu, not the page. `.site-header` is `position: sticky` with no height cap; the checkbox-revealed `.site-nav` had none either. A sticky element has no internal scroll, so content below the fold inside it was **permanently** unreachable, not merely hard to reach — confirmed by scrolling the page 1000px with the menu open and finding Contact's on-screen position did not move at all. **Contact, the last item in the nav list, was unreachable on a phone for the entire period** — on the highest-traffic device class, on the page the whole nav exists to route people toward.
  **Measured before fixing, not assumed:** at 390px width (Buyers, EN), viewport 664px, open header height 1067px, `.site-nav` 859px tall, both `max-height: none; overflow-y: visible`. Contact sat 989–1041px down — entirely below the fold — in both English and Spanish, at 390px, 360px, and 320px.
  **Fixed with a self-adjusting escape hatch, deliberately not a fixed-pixel one.** The first fix proposed (Lisa) was `max-height: calc(100vh - <header bar height>)`. Measurement showed why that would have been wrong immediately and stayed wrong: the closed header bar is **188px in English but 227px in Spanish** at 390px width (the Spanish CTA text wraps to two lines), and differs again at 320px (227px EN / 243px ES). A pixel constant tuned for one language, one breakpoint, and today's copy would have silently drifted the next time any of the three changed — the same failure shape as `audit.js` check 28's dead threshold branch, one component over. Shipped instead: `.site-header:has(.nav-toggle__box:checked) { max-height:100vh; overflow-y:auto; }`, which caps the whole open header as one scrollable unit with no number to keep in sync, plus an `@supports not selector(:has(a))` fallback capping `.site-nav` itself at a generous `70vh` for the small remaining set of browsers without `:has()` support (shipped in every evergreen mobile browser since 2022).
  **Proven reachable, not claimed — same measurement, after the fix:** at all six combinations (390/360/320px × EN/ES), scrolling the open header's own internal scroll container to its max brought Contact's link fully on-screen (`top:722 bottom:774` in every case, against a 799px capped viewport). Verified against the real, minified, committed `assets/css/site.css`, not a prototype.
  **Two alternatives considered and rejected, reasoned, not just asserted:** un-sticking the header at mobile widths would also fix reachability but throws away the toggle staying reachable while scrolling a long article; collapsing the Communities/Resources sub-lists behind their own toggles would shrink today's overflow without fixing the defect class — three more Goshen spoke articles are already queued (see the item below), so the list grows again regardless.
  **New `audit.js` check 26 → renumber correction: this is check 29** (checks 26–28 shipped in PR #118 the same week). Static, text-based, matching every other check in this file — verifies `assets/css/site.css` contains a bounded-height + scrollable rule either on a `:has(.nav-toggle__box:checked)` ancestor or on the toggle-revealed panel itself. **Proven three ways:** true positive against the actual pre-fix file from `origin/main` (not a planted example — the real file that shipped the bug); true negative against the fixed file; a mutation test flipping `overflow-y: auto` to `overflow-y: visible`. **Honest nuance surfaced by the mutation test itself:** the shipped fix has two independent, redundant escape hatches by design (the `:has()` ancestor rule and the `@supports` fallback) — breaking only one still leaves the site reachable via the other, so the check correctly stays green in that case too. Only breaking **both** reproduces genuine unreachability, and that is what the mutation test actually does.
  **Scope limit of this check, stated plainly rather than left implicit:** it verifies the CSS structurally guarantees an escape hatch, which is sufficient *only because* the shipped fix is self-adjusting. It could not have caught the originally-proposed `calc()` approach failing at some future breakpoint or label length — that needs a rendered-viewport measurement, which is the separate gap tracked immediately below, deliberately not built here.
- [ ] **No check in this project renders a page and measures it — every existing check, including the new one above, reads HTML/CSS as text. Tracked as a gap, not acted on (Lisa, 13 August 2026).** The mobile-nav bug was invisible to static analysis by its own nature: nothing about the CSS *text* said "this will overflow the viewport" — that only became knowable by actually rendering the panel at a real width and measuring it, which is exactly how it was eventually found and proven (Playwright, run manually, not part of `npm run audit`). Check 29 closes this one instance because the shipped fix happens to be self-adjusting; it is not a general answer, and a differently-shaped future defect (or a future fix that reintroduces a fixed pixel constant) could be just as invisible to every check that exists today. **Whether to take on a rendered-viewport dependency in the audit pipeline — new infrastructure (a headless browser, slower CI, cross-platform reliability) this project has never carried — is a decision for a later session, with its own report before anything is built.**
- [ ] **Three Goshen spoke articles are Phase 2 candidates, not gaps (Lisa, 5 August 2026).** From the coach's original ten-article proposal in the `/moving-to-goshen/` Internal Link Map, three were never built and are genuine backlog: **Goshen Neighborhoods and Housing Styles**, **Schools in Goshen: Districts, Campuses, and How to Research Them**, and **New Construction in Goshen: What to Ask a Builder**. Recorded here so they are tracked, and recorded in that file as backlog so they are not read as missing pieces of a finished cluster.
  **Two others from that list need nothing:** "Goshen or Elkhart" is covered by a section on the pillar itself, and "Historic Homes in Goshen" is covered by the Buying an Older Home article. Both marked **covered**, not missing.
  **The cluster is complete at six spokes.** Note that both spoke titles #1 and #2 were originally proposed as "Best Neighborhoods" and "Best Schools" and were retitled at draft time because ranking areas by quality is the pattern that produces steering complaints — if these are ever built, the compliant titles are the ones to build.

## D. EN/ES parity backlog

- [x] **Home page `faq-item--media` asymmetry — found 17 August 2026, pre-existing, unrelated to the body-image build that surfaced it; fixed same PR (#126), on Lisa's explicit ruling not to defer.** `npm run audit` (check 14) flagged Home's image count moving from the recorded baseline (15/11) after the five new body images were wired — expected, since the new images are symmetric (5 EN / 5 ES, `section-figure` 3/3 confirmed by the audit's own breakdown). The **unexplained part of the count was `faq-item--media: 4` on EN vs `0` on ES** — a pre-existing gap the old 15/11 baseline was already carrying (4-image gap either way), just never named. First reported as "logged, not fixed here — out of scope for this PR"; Lisa ruled that framing out and required the fix in the same PR, since the FAQ gap is on a page #126 was already editing in both languages.
  **Fix applied:** the same four `blog-headers/*-thumb` files EN already used were added to `es/index.html`'s four matching FAQ entries (`faq-item--media`, left/right alternating, linking to the Spanish articles the ES FAQ already referenced), each with a newly drafted Spanish alt string approved by Lisa. English alt text was checked against the same files' alt text on their own EN blog article pages before being trusted as good (Alt-Text Correspondence Standard) — all four matched and were accurate, so none were rewritten.
  **Alt text was drafted as direct description, not Volume 37 cultural adaptation — a distinction worth stating for future sessions.** Volume 37's never-translate rule governs Lisa's own writing (body copy, headlines); alt text describes what is visible in a photograph, a different class of content, so a straightforward Spanish description of the same photo is correct — the same logic already applied to the Review Language Standard's translated-review labels, extended here to a second content class.
  **Baseline re-derived from fresh measurement, not incremented from the interim 20/16 figure recorded when the gap was still open:** `grep -c '<img '` on both live files now returns **20 EN / 20 ES** — true parity. `content/image-parity-baseline.json`'s Home entry updated to 20/20.
- [ ] **24 baselined mismatched pairs** recorded in `content/image-parity-baseline.json` — mostly Spanish blog articles missing their featured image. This is a **worklist, not an exemption**: `audit.js` check 14 errors if a baselined gap changes in either direction, so the baseline can only shrink. None of the four community pages is in it. *(**24** as of PR #107, which deleted the four resolved Elkhart entries; still 24 after the 17 August blog-listing-images build, which added one new pair — `/blog/` 61 vs `/blog/spanish/` 47 — the same day it closed none, both measured, not carried forward.)* The new pair's gap is the 25 articles with no featured image at all (§A0), not a defect in the wiring itself — it closes automatically as thumbnails are generated post-launch, no baseline edit needed to shrink it since `audit.js` only requires the *current* count to match, not a specific reason.
- [x] **`/es/vendedores/` review-block claim — CLOSED AS FALSE, 11 August 2026. The block was never missing.** This entry originally claimed "What Lisa's clients say" had "no Spanish counterpart at all." **Verified false, directly against the live page and against `git log`, not against this entry's own prior description of itself.** A Spanish counterpart — "Lo que dicen sus clientes" (Cindy Ortiz photo review, Felicita Donoso text-only, a generic couple photo) — has existed on `/es/vendedores/` since **27 July 2026 (PR #71, commit `aac21e5`)**, the same commit that built `/sellers/`'s "What Lisa's clients say" block. Both blocks have existed, symmetrically, since the page pair was built; there was never a moment where one existed without the other.
  **Root cause, traced through git log — not a placeholder.** The false claim originates in **commit `014f102`, "Close the 501 Danbury Dr EN/ES gap on the sellers page," 30 July 2026 07:39 UTC** (part of the branch merged as PR #98; this entry's "Found 31 July" is off by a day from the actual commit, corrected here). That commit's own message states, verbatim: *"EN carries an extra 'What Lisa's clients say' review block ES doesn't have."* The commit's diff (checked directly) **only touches the sold-quote-card block and `content/image-parity-baseline.json`** — zero lines touching "Lo que dicen sus clientes." Whoever wrote it was fixing an unrelated gap in the same file and stated something untrue of the file they were editing, without checking it.
  **This commit is also where the baseline entry's reasoning first appears** — the 31/32 image-count baseline (`content/image-parity-baseline.json`) was added in this same commit, justified by this same false sentence. Correcting a description carried in the prior version of this entry: that baseline reason is not "unrelated" to this claim — it's this claim's origin.
  **What was actually verified, in full, on the live block:** both cards are §17 (Review Language Standard)-compliant. Cindy Ortiz (original English, per `content/reviews.json`) carries the `"Traducido del inglés"` label; Felicita Donoso (original Spanish) correctly carries no label. Image count in this specific block is equal on both language sides — 2 and 2 (Naun+generic on EN, Cindy+generic on ES). **No content needs to be built here.**
  **The baseline-masking claim was also false, for a related reason.** The baseline was never masking a gap in this review block, because this block was never unequal. The real +1 in the `/sellers/`↔`/es/vendedores/` baseline (31/32) traces to the main sold-listings showcase grid (13 ES listings vs 11 EN) — a genuine, unrelated substance-parity gap under the EN/ES Parity Standard, logged as its own new item below, not folded into this one.
- [ ] **`/es/vendedores/`'s main sold-listings showcase shows 13 listings; `/sellers/`'s shows 11 — logged, not investigated.** Found while tracing the review-block baseline above (11 August 2026). This is the actual source of the `/sellers/`↔`/es/vendedores/` image-count baseline's +1, not the review block. A substance-parity gap under the EN/ES Parity Standard — two more sold listings shown on the Spanish page than the English one. Not investigated further per instruction; queued for a future look.
- [ ] **`.visually-hidden` "Artículos relacionados" heading exists on all 27 Spanish pages that use the pattern, and on zero English pages.** Found during the check-25 screen-reader-text sweep (11 August 2026). Not a language mismatch (nothing wrong-language is rendered) — a possible content-completeness gap: either English pages should carry an equivalent hidden "Related articles" heading and don't, or the ES-only pattern is deliberate and undocumented. Not investigated further; needs a decision on which.

## E. Copy & compliance

- [x] **LIVE VIOLATION, FIXED 18 August 2026 — banned phone number 574-975-0141 was legible on `/blog/community/what-is-it-like-to-work-with-lisa-collio/`'s featured image, live, both languages' `og:image` inheriting it. Found 17 August 2026 during an unrelated blog-images task; predates that task.**
  **How it got there, in one sentence, because this is the part worth remembering:** the source file's name — `lisa-collio-open-house-flag-summer.jpg` — gives no signal at all that a phone number is anywhere in the frame, so every filename-based sweep for "sign," "banner," "flag," "graphic," "courthouse," "listing" walked right past the fact that this particular "flag" photo also has a yard sign with the banned number in it, at the very bottom of the frame. The sweep found six loose assets by filename; it could never have found this one, because this one wasn't a loose asset that looked like signage — it was already load-bearing as a live page's featured image, and the giveaway (a yard sign) was incidental background in a photo about something else (an open-house flag).
  **Where in the frame:** roughly x 11–14%, y 90–94% of the delivered 1200×630 crop — bottom-left corner, in the last ~10% strip of the frame.
  **Fix:** added a `crop_box` of `(0.0, 0.0, 1.0, 0.75)` to the `what-is-it-like-to-work-with-lisa-collio` entry in `scripts/generate-featured-images.py`'s `CLEAN_JOBS` table — restricts the source to its top 75% before the existing `focal_y=0.45` cover-crop runs. Rebuilt with `--clean`; the regenerated header and thumb (plus both `.webp` siblings) were checked directly, at full delivered resolution, not assumed clean from the crop math alone: **no phone number appears anywhere in the frame at all — not the banned one, not the correct one.** Lisa, the open-house flag, and the full house are all still intact; only the ground and the very top edge of the yard sign's red band survive, with no text readable on it. `hash-assets.js` propagated the new content hash to all three pages that reference this file (the article page itself, `/blog/`'s listing card, and `/about/`'s FAQ media block) — a genuine third reference the fix had to reach that a narrower "fix the one page" pass would have missed.
  **A second, related finding surfaced only while rebuilding, not by any sweep:** `moving-to-goshen-indiana-from-out-of-state` (a different article) also used the now-deleted `lisa-collio-for-sale-sign-summer.jpg` as its `CLEAN_JOBS` source — the exact file removed in the prior PR for carrying the same banned number. Running `--clean` after that deletion crashed on a missing file. Checked that job's own already-built, already-live header at its own `focal_y` (0.35, different framing from the job above) directly, not assumed clean because the other job turned out clean: **the number sits below this delivered frame's bottom edge, not in it — this one was never a live violation.** Since the source is gone for cause and must not be restored, the job-table entry was changed to a new `SOURCE_DELETED` sentinel (parallel to the existing `FROM_COMPOSITE` pattern) that makes `--clean` skip it with a printed note instead of crashing, while leaving the correct, already-built delivered files untouched and the Build Record Standard intact — the entry still states what happened and why, just not a reproducible source path anymore.
  **What `SOURCE_DELETED` (and `FROM_COMPOSITE`, the pattern it copies) actually means, stated plainly rather than left for a future session to reconstruct from the sentinel's code: the sentinel prevents `--clean` from crashing. It verifies nothing, and it adds no safeguard.** Three `CLEAN_JOBS` entries are in this position — not just the one added today:
    - `moving-to-goshen-indiana-from-out-of-state` (`SOURCE_DELETED`, added today) — source deleted for cause, cannot be rebuilt.
    - `does-lisa-collio-speak-spanish` (`FROM_COMPOSITE`) — source was consumed one-way when first built; cannot be rebuilt.
    - `lisa-collio-helps-seniors-families-downsize` (`FROM_COMPOSITE`) — same, cannot be rebuilt.
    For all three: nobody can regenerate their output and diff it, because there is nothing left to regenerate it *from*. Each one's correctness rests entirely on a one-time manual visual check, not on any mechanical rebuild-and-verify. For `moving-to-goshen-indiana-from-out-of-state`, that check happened twice, both on 18 August 2026 — the live-referenced file (`moving-to-goshen-indiana-from-out-of-state-header.jpg?v=d2de6490`, unchanged since PR #71) was viewed at full resolution and the bottom edge specifically zoomed 6x to check for a partial phone-number line: none is visible, banned or correct. That is the state of the record — **unverifiable by tooling, confirmed correct by inspection on 18 August 2026** — not "safe because it was fixed."
    There is also a real, narrow gap this creates in `audit.js`: **check 23** (the alt-text correspondence check, which cross-checks alt text between two derivatives of "the same photograph" using `CLEAN_JOBS`/`SECTION_JOBS` as its source of truth) explicitly skips any job whose recorded source isn't a real path — `if (!alt || !src || !src.includes("/")) continue;`. All three of the jobs above fall into that skip by the same existing logic, so none of them is covered by that cross-check either. This was true of the two `FROM_COMPOSITE` jobs before today; it is not a new hole, but it was never written down as a standing fact about this class of entry until now.
  **This is not a fixed set of files to clean once.** The banned number is printed on physical yard signs and flags Lisa still owns and uses. Any future photo of that signage — at an open house, a listing, a sold celebration — carries the same problem, regardless of what the photo is *of* or how it gets named. This needs to be a standing check applied to new photography as it comes in, not a closed incident.
  **The sweep gap, recorded in Lisa's own words: the honest claim is "the checked images are clean," not "all images are clean."** Three named, un-closed gaps:
    1. **~35 files in `assets/images/lisa/` and `assets/images/client-general/` whose filenames give no signage signal at all** — convention/expo photos, award-wall shots, family and holiday photos, casual selfies. Any could have a yard sign, banner, or printed card incidental in the background. None of these were opened.
    2. **67 real listing photos in `assets/images/homes-elkhart/`/`homes-goshen/`** — one `card-photo.jpg` spot-checked (clean, professional MLS photography with no marketing text baked in), not verified as a class.
    3. **The keyword list is its own ceiling.** Every sweep run so far (filenames containing sign/banner/flag/graphic/courthouse/listing, then business-card-adjacent terms) is only as good as the words chosen. This exact violation proved the ceiling is real: "open-house-flag-summer" matched the keyword sweep on "flag" and still wasn't caught, because the sweep checked filenames for signage-shaped words, not frames for signage-shaped content.
  **OCR across the full image set is the real fix for the sweep gap, and it's a separate decision with its own cost** — logged here as a **post-launch item, not a pre-launch blocker.** One wrong number surviving in a background photo is a small, one-line-fixable harm if and when it surfaces, not a launch-blocking risk proportional to the cost of auditing roughly 100 more images by hand today.

- [x] **Review-card quote marks unified sitewide, and a rotating thank-you-message card replaces the static "Lisa's clients" caption — DONE 17 August 2026.** Report-Before-Build: Lisa reported one broken card (a floating opening quote with no closing pair on the text-only variant); the Phase 0 recon found the actual scope was a field class, not one card — **three different, inconsistent behaviors** for the same visual element: the text-only card's oversized floating `::before` (no closing pair), the photo card's variant which had **no quote mark at all**, and — found only once the recon widened past the one card Lisa had seen — **eight pages** (Home, Buyers, Sellers, About, and their ES twins) carrying hand-authored "featured review" blocks outside the rotation system with literal quote characters typed directly into the markup (curly `" "` on English pages, guillemets `« »` on Spanish pages, confirmed by direct inspection, not assumed identical to English).
  **Fix:** CSS `quotes: "\201C" "\201D"` plus `::before`/`::after: open-quote/close-quote` on the shared `.review-card blockquote` selector — wraps the actual text (opens before the first character, closes after the last) on every review card sitewide, both languages, zero markup changes needed in `build-reviews.js`'s 135 rotation-managed pages. The old oversized floating glyph and its now-unnecessary compensating top-padding were removed from `.review-card--textonly`. The 19 literal quote characters across the 8 hand-authored pages were removed from the markup so they don't double up with the new CSS.
  **Thank-you rotation:** the review block's third card (a client-general photo captioned "Lisa's clients"/"Clientes de Lisa," blockquote always empty) now shows a rotating thank-you message in Lisa's own voice, attributed "— Lisa Collio," sourced from `content/thank-you-messages.json` (15 EN + 15 ES, extracted verbatim from `docs/approved-copy/English_Thank_you.docx` / `Spanish_Thank_you.docx`). The photo and its existing rotation are unchanged.
  - **Rotation mechanism — separate and lighter than the reviewer rotation, deliberately.** Reuses only the existing adjacency graph (so the same sentence doesn't land on two pages a visitor can click between); carries none of the person/household-banning machinery, because that exists specifically because reviewers are identifiable people, and none of it applies to Lisa's own marketing copy.
  - **Per-page and committed, matching the existing reviewer rotation — and here is the reason, on the record for the next session that proposes per-load instead: this site ships no client-side JavaScript anywhere.** `Content-Security-Policy: script-src 'self'` and there is no `<script>` file in the repo beyond inline JSON-LD. A per-load rotation would require adding a JS runtime to a site that has deliberately never had one. That is the actual argument, not just "matches the existing pattern" — a future session finding this worth reconsidering should read this line before proposing a client-side rewrite.
  - **Visual distinction from real reviews — three stacked, all confirmed present, none alone would have been enough:** no star rating (the markup never calls `stars()` for this card), no platform-source tag (no `Google`/`Facebook`-style `<span>`), and a RE/MAX-Blue-tinted background (`.review-card--thankyou`) distinct from both the plain-white photo/text cards and the navy-tinted text-only card. A five-star rating beside Lisa's own copy would have been exactly what reads as a fabricated review.
  - **STANDING RULE, recorded here because nothing else stops a future session from applying it mechanically: the thank-you card must NEVER carry a "Translated from X" label.** That mechanism exists under the Master Plan's Review Language Standard (§17) for actual client-review translations only. These messages are independently authored in each language (Volume 37: Lisa's own writing is cultural adaptation, never translation, same as everywhere else on the site) — applying the translation label here would misrepresent Lisa's own Spanish writing as derivative of her English, which is backwards, and is the exact inverse of what §17 exists to prevent. `content/thank-you-messages.json`'s own `_README` repeats this; recorded twice deliberately, same reasoning as why the Spanish-phone-form ruling lives in both CLAUDE.md and here.
  - **Grid-stretch measured, not assumed clean.** CSS Grid's default `align-items: stretch` was a real risk with a longer message replacing a two-word caption. Measured with Playwright across four pages (Home, `moving-to-goshen`, `buyers`, `es/vendedores`, `contact`): the thank-you card's natural (unstretched) height is 439–465px against a 502–716px stretched row height — closer to the photo card's natural height than the pre-existing text-only card's natural height (222–336px) already is. The new card does not introduce a worse stretch gap than the site already tolerates; no grid CSS change made.
  - **Side effect: closes the 109-stale-rotation-pages item below.** This build ran `node build-reviews.js` in write mode across all 135 rotation-managed pages (every one changed, since every one gained the new third-card markup regardless of prior staleness), which incidentally re-synced every previously-stale page's reviewer assignment to current. See that item, marked closed.
- [x] **Alt-text banned-terms sweep — DONE 29 July.** Built as `audit.js` check 17, EN + ES, from `content/source/fair-housing-terms.txt`, covering all five categories. Fixed on the way: "tranquila"/"quiet" in the daily-life alt text (the English twin carried the same term and had not been flagged), plus a material claim and an architectural classification. The alt sweep now passes across all 164 pages.
- [x] **Named-organization consistency in alt text — SETTLED 29 July.** Standard: describe the building or scene, never name the organization. "Goshen Historical Museum" removed (PR #79); "Cámara de Comercio y Centro de Visitantes de Goshen" removed in both languages (PR #83 — the English twin named it identically and had not been flagged); **"Old Bag Factory" is deliberately kept** as a landmark building, per Lisa's ruling that the building stays and only its tenants come out.
- [ ] **`/moving-to-goshen/` has 6 FAQ questions, 5 images** in both languages. This is correct — the sixth is a rejected asset. Recorded here so it isn't "fixed" by a future session. Closes only when a replacement photo arrives.
- [x] **Meta and og/twitter description sweep — DONE 29 July (PR #87).** 279 description-class fields across 156 pages, both languages, against the full locked identity block and the Fair Housing list. **Five real violations, all fixed:** four buyers meta descriptions running "Realtor Lisa Collio" (REALTOR® without the mark, and leading with the designation), and `blog/community/` describing the service area as "Elkhart County towns". Clean everywhere else: phone correct in all 38 occurrences, no superseded number, no "Northern Indiana", no `REMAX`, and **zero** Fair Housing hits. The Elkhart County 4-H Fair and the Elkhart County Board of REALTORS® MLS were left alone as correct proper names.
- [x] **Audit check for descriptions — DONE 29 July.** Built as `audit.js` check 18, scoped to description fields rather than sitewide, on measurement: the same REALTOR-® rule applied to whole pages produces 238 hits across 93 pages of which essentially none are real — 222 are lowercase "realtor" inside **verbatim client reviews that must never be edited**, and 16 are `REALTOR®` correctly escaped in JSON-LD. Scoped to descriptions it found exactly the four real ones with no noise.
- [x] **Title-suffix inconsistency — three remaining titles identified 29 July.** Decision still open, but the list is no longer unknown. **None of the three can take the suffix as written** — all land over the 60-character ceiling, and abbreviating "Indiana" to "IN" rescues only the first, to 61, still over. Normalising means shortening the titles themselves, which is a copy decision:
  | title | now | with `\| Lisa Collio` |
  | --- | --- | --- |
  | How to Compete with Other Buyers in Elkhart, Indiana | 52 | 66 — over |
  | What to Look for When Buying a Home in Elkhart, IN | 50 | 64 — over |
  | What to Prioritize When Buying a House in Goshen, IN | 52 | 66 — over |
- [ ] **No `og:description`, `og:title`, `twitter:description` or `twitter:title` anywhere — 0 of 156 pages.** Found 29 July while sweeping for the item above. Pages carry `og:image` and `twitter:image` only, so Facebook and LinkedIn fall back to the meta description. Not a rule violation, but it has two consequences: share copy cannot be tuned separately from search copy, and **the meta description is live advertising copy on social**, which is what made the "Realtor Lisa Collio" finding more exposed than a SERP snippet alone. Decide whether to author these fields; `audit.js` check 18 already covers them if they appear.
- [x] **Phone-number convention — SETTLED AND APPLIED 30 July.** Two forms only: EN `Call or text (574) 370-5410.` and ES `Llame o escriba al (574) 370-5410.` The eight prior phrasings collapsed to those two. Placed last in the sentence deliberately, because the tail is what Google truncates first. **Scope: high-intent pages only** — the six Tier 1 pages, Contact, and Market Stats — not the authority articles, where a reader is researching rather than calling.
  - **Priority when space is tight: description content wins.** Where the phone would not fit inside 145–160 without cutting substance, it was dropped for that page. That happened once: `/es/conozca-a-lisa/`, where the Spanish form costs 35 characters and the description would have lost a third of its content. Left unchanged and phone-free.
  - **`/contact/` and `/es/contacto/` are HELD, not normalised** — see the office-address item below.
  - ⚠️ **RULING (Lisa, 5 August 2026) — the two Spanish phone forms are a deliberate field-class difference, not drift. Do not "fix" one to match the other.**
    | Field class | Spanish form | Cost |
    | --- | --- | --- |
    | Meta / og / twitter / schema **descriptions** | `Llame o escriba al (574) 370-5410.` | 34 chars |
    | **On-page CTAs** (CLAUDE.md's locked form) | `Llame o mande un mensaje de texto` | 33 chars + number |

    **The reason is the 145–160 band, and it is arithmetic, not preference.** The CTA form plus the number costs roughly 50 characters of a 160-character ceiling — nearly a third of the description — and would force real content out of every Spanish description carrying it. The description-length priority rule already in the Master Plan settles the conflict in content's favour, so the description form is deliberately shorter. Both forms say the same thing to a Spanish reader; only one fits.
    Recorded here **and in CLAUDE.md** because this is exactly the shape a future session mistakes for inconsistency: two field classes, same language, same fact, different strings — which is what the field-class coverage rule normally tells you to go and unify. This is the documented exception to that instinct. A field-class difference with a stated reason is a decision; the same difference without one is drift.
- [x] **Sold-listing consent — CLOSED 2 August 2026. Folded in from the branch copy of v2.8 on 5 August, recorded as history rather than dropped.**
  **What the item said while it was open:** `content/source/sold-listing-consent.txt` (added in PR #98, enforced by `audit.js` check 21) lists the 14 addresses claimed as "Sold by Lisa Collio" / "Vendida por Lisa Collio" across `/sellers/` and `/es/vendedores/`. The file was an accurate **inventory** of what was live, seeded from the pages as they existed 31 July — explicitly **not** a certification that written seller consent (876 IAC 8-1-8(f)) was on file for each one. The check only guarded against a *new* address shipping without a deliberate manifest entry; it could not retroactively verify the 14 already there.
  **Resolution: Lisa confirms signed written seller consent is on file for all 14 addresses (2 August 2026).** The file header's "not yet independently re-verified against Lisa's consent records" caveat is removed and the confirmation recorded in its place. Check 21's gate is now backed by actual records rather than a starting inventory. Verified the file still parses to exactly 14 addresses after the header edit.
  **Why this is written down instead of simply deleted:** this item was the *only* record anywhere that the consent file was an inventory and not a clearance, and it spent five days on an unmerged branch. Retiring that branch without reconciling it would have silently deleted a compliance caveat attached to live advertising copy — no diff, no warning, and the file would have read as a certification it had never been. That is the Single Open-Items Record Standard's failure mode with a compliance consequence attached.
- [x] **Meta-description band — DONE 30 July.** All descriptions now sit inside 145–160. **Correction: there were 32 out-of-band descriptions, not 41.** The 41 figure recorded in v2.1 was the total SEO warning count, which also included 9 over-length titles — a mistake in the v2.1 entry, carried into the decision to do this pass. The decision holds either way. Audit SEO warnings dropped 41 → 9, and the 9 remaining are titles.

- [x] **Office address in the two contact descriptions — SETTLED 30 July: deliberate exception, keep as-is.** `/contact/` (154) reads *"Contact Lisa Collio, Real Estate Agent in Goshen and Elkhart, Indiana. Call or text (574) 370-5410, email, or visit the office at 1918 Elkhart Rd, Goshen."* and `/es/contacto/` (160) *"…Llame o mande un mensaje de texto al (574) 370-5410 o visite la oficina en Goshen."*
  **Do not normalise these two to the settled phone forms, and do not strip the address.** Lisa's ruling: the full NAP on a Contact page is intentional, not leftover. Both are already inside 145–160 and both pass `audit.js` check 18, so nothing flags them today — which is exactly why the exception is written down here. A future "collapse the phrasings to two forms" pass would otherwise look at them and tidy them away.
- [x] **Over-length titles and the buyers-cluster suffix — BOTH CLOSED 30 July.** The two were separate sets with **zero overlap**: the nine were all over 60, the three buyers titles were 50–52 with no suffix.
  **Six of the nine were never over 60.** `audit.js` counted raw markup, so `&amp;` scored five characters instead of one — overstating six titles by four each. Fixed by decoding entities before measuring, the same `decode()` the identity checks already used. Those six are unchanged; rewriting them would have meant changing good copy to satisfy a broken ruler.
  Three were genuinely over and are rewritten: the RV-industry article (78 → 60), `/es/disclaimers/` (67 → 57), `/es/privacidad/` (61 → 51). The three buyers titles gained the suffix at 55–57.
  **Title tags only for the Spanish and buyers titles** — no H1, schema, or image changes needed there. The three buyers H1s are questions and never mirrored their titles; the two Spanish H1s are unchanged because only the suffix shortened.

  **The RV-industry article is different, and was corrected twice.** This entry first said aligning its H1 would force regenerating 4 featured-image files, because the slug was assumed to carry baked-in title text. **That was wrong** — the slug is not in `OVERLAY_SCOPE`; its featured image is a clean photo (`CLEAN_JOBS`), no text baked in. The error came from grepping the slug anywhere in `generate-featured-images.py` and getting a hit, without checking which table matched — the hit was the `CLEAN_JOBS` entry. Once corrected, aligning cost one line. **DONE 30 July:** H1, schema `headline`, breadcrumb name, and card-link text in `blog/community/index.html`, `blog/community/elkhart-indiana-industries-employers/index.html`, `blog/index.html`, and `sources/index.html` all now read *"Buying a Home in Elkhart on an RV Industry Job"*. No page still quotes the old H1.

  **Lesson:** a grep match against a script is not evidence of *which* table matched. Verify by parsing the specific block (`OVERLAY_SCOPE`, `CLEAN_JOBS`, `SECTION_JOBS`), not by grep against the whole file.

- [x] **109 pages had stale (not broken) review-rotation state — CLOSED as a side effect, 17 August 2026.** Found 30 July while verifying content for the Audit Checklist fold, via `node build-reviews.js --check`; corrected from an under-measured 53 to 109 on 11 August (see below — the re-measurement history is kept as the record of why a stale count must never be cited forward without re-checking). **Closed not by a dedicated fix but as a side effect of the review-card quote/thank-you-message PR**, which ran `node build-reviews.js` in write mode across all 135 rotation-managed pages — every page's reviewer assignment was re-synced to current, since every page also needed the new third-card markup regardless of whether its rotation state was already stale. Re-verified: `node build-reviews.js --check` reports all pages up to date. History below kept as the record of the root cause and the re-measurement lesson, not because the item is still open.
  **The 53 figure was measured once, 30 July, and cited forward without re-measurement — this is exactly the failure the Verification Standard and the Changelog Citation Standard both warn about, just for a count instead of a claim.** Re-measured directly via `node build-reviews.js --check`, 11 August 2026: **109 pages**, not 53 — more renames/merges in the intervening eleven days cascaded more adjacency staleness through the graph. **The lesson is the re-measurement requirement, not the number itself: this figure is only ever correct as of the date it was last measured, and must be re-measured before being cited again, not carried forward on trust.**
  Breakdown as of 11 August 2026: 11 `blog/buyers/` · 13 `blog/sellers/` · 28 `blog/community/` · 2 `blog/market-updates/` · 47 `blog/spanish/` · 6 non-blog (`living-in-goshen/`, `market-stats/`, `moving-to-elkhart/`, `es/viviendo-en-goshen/`, `es/estadisticas-del-mercado/`, `moving-to-elkhart/`'s ES twin) = 109. `/sellers/` and `/es/vendedores/` are both current, not in this set.
  **Root cause, traced through git history (30 July finding, still accurate for what it explains):** the last full `build-reviews.js` write-and-commit was the Wave 2 commit (`fe7701e`, 27 July). Since then, PR #86 renamed `what-makes-goshen-indiana-desirable-place-to-live` to `is-goshen-indiana-good-place-to-buy-home` — the rotation algorithm keys adjacency and least-recently-used order off page path, so the rename reset that page's rotation history, cascading staleness to every page adjacent to it in the graph (nav-linked, hub-linked, hreflang twin). That's why so many otherwise-unrelated English and Spanish articles went stale together rather than a scattered few. Further renames/merges since 30 July are the likely cause of the growth to 109, not independently traced page-by-page.
  **Fix:** one `node build-reviews.js` run (write mode) + a PR, whenever convenient. Not urgent — no launch-blocking or compliance issue, just tracked so it isn't lost. **Deliberately not run as part of the aria-label fix (PR pending, 11 August 2026)** — see the new §B entry — to avoid quietly restating 109 pages of review content inside what was scoped as a two-word accessibility fix.

## F. Verify prior batches actually landed

Referenced in project memory but never directly re-confirmed.

- [x] **Batch 2b — CONFIRMED LANDED, 11 August 2026, first direct verification.** Found while deleting the batch's instruction file (§B, Master Plan v2.20 bundled bump). **What was checked:** `build-reviews.js` and `content/reviews.json` both exist and are live on `main` — confirmed directly, not inferred, having just edited both files in PR #115 immediately prior. **What was not re-checked:** the specific sub-claims in this box's original text — "62 reviews" as an exact count, and "no repeats on same or adjacent pages" — were not independently re-verified here. The adjacent-page-repeat check is a separate, still-passing mechanical check (see §B/§D's review-rotation entries), which is evidence for the second claim but wasn't re-run specifically for this closure.
- [x] **Batch 2c — CONFIRMED LANDED, 11 August 2026, first direct verification.** Found the same way. **What was checked:** all four Spanish legal pages exist live on `main` — `es/privacidad/`, `es/terminos/`, `es/accesibilidad/`, `es/vivienda-justa/`, confirmed by direct directory listing. **What was not re-checked:** the "ES footer and consent-checkbox link fixes" portion of this box's original text was not independently re-verified — only that the four pages themselves exist.
- [ ] Confirm PR 12's Fair Housing sweep reached production, not just a deploy preview

## G. Technical debt — before DNS cutover

- [x] **Content-hashed asset filenames — CLOSED 17 August 2026, by query-string hashing (Option B), not by renaming files on disk (Option A).** PR #73 shipped the short-term fix (shorter max-age + must-revalidate per subtree) after `/assets/` was found running year-long `immutable` caching on non-hashed filenames. The durable fix was scoped under Report-Before-Build: a Phase 0 recon measured the actual reference surface before anything was built, rather than assuming the conventional `site.a3f9c1.css`-style rename-and-rewrite was the only option.
  **Why Option B (append `?v=<content-hash>` to every reference, physical filenames unchanged) over Option A (rename the file, hash in the filename itself) — this is the reasoning worth keeping, because the next person will ask why this site does not use hashed filenames the conventional way.** This site has no build-time render step — Netlify serves the committed repo directly (`netlify.toml` has no `[build]` command) — so every image/CSS reference is either hand-authored literal HTML or generated offline and committed as real bytes; there is no template layer a rename-and-rewrite could hide behind. Checked against the actual regexes before choosing, not in the abstract: `audit.js` check 2's href/src pattern already stops capturing at `?`, so a query string is invisible to it and needs no change; checks 15, 22, and 23 all match bare filenames on disk or in HTML and would have gone blind on every rejected asset, every section image, and every alt-text-correspondence group the moment a physical filename changed, needing coordinated fixes in the same PR under Option A. `content/source/rejected-assets.txt` and the four Python job tables (`HERO_JOBS`/`SECTION_JOBS`/`OVERLAY_JOBS`/`CLEAN_JOBS`) are all keyed by canonical un-hashed slug as their source-of-truth identifier — Option B needed zero changes to any of them; Option A would have needed every one updated correctly, simultaneously, with a partial miss serving stale content invisibly rather than failing loudly. Lisa's accepted trade-off: query-string busting is a marginally weaker convention than a hashed filename on legacy corporate-proxy networks that ignore query strings for caching — not a real risk for this site's mobile-majority residential buyer/seller traffic.
  **What shipped:** `hash-assets.js` (new) rewrites all five reference patterns the Phase 0 recon inventoried — href/src, srcset, og:image/twitter:image meta content, JSON-LD `"image"` fields, and the `--hero-img` CSS custom property (both its rendered form and the separate bare-JSON-string form inside each Tier 2 page's `build:hero-tier2` marker comment — a sixth, closely-related pattern the recon hadn't separately named, caught during this build's own verification pass when the hero count came in 22 of 24 instead of 24 of 24). Runs after `minify-css.js` and `build.js` per Lisa's Phase 1 instruction, rewriting every literal occurrence of an asset path anywhere in a file's text — including component partial sources (`components/header.html`, `footer.html`, `header-es.html`, `footer-es.html`) and marker JSON comments, not just rendered output — so a later, solo re-run of `build.js` alone re-renders from an already-hashed source and never regresses. `build-reviews.js` is a second, independent code path that generates review-card and client-general-photo markup programmatically from a directory listing on every run; it now computes its own hash via the same `hash-util.js` function (`sha256`, first 8 hex chars) rather than relying on a downstream patch, so a solo re-run of `build-reviews.js` alone also never regresses. Both scripts produce the identical hash for the identical file bytes by construction — no coordination between them beyond sharing one function.
  **Counts, reconciled against the Phase 0 recon's own numbers, not just reported as a success:** the recon's raw grep of `assets/...` occurrences across HTML reproduced exactly (2,734 raw, 499 distinct) both before and after this PR, confirming no reference was corrupted, added, or dropped — a `?v=` suffix is invisible to that grep pattern by construction. Of the 2,734: 312 are `<link rel=preload>` font references, deliberately out of scope (fonts are referenced only from `@font-face url()` and preload links, outside the five patterns this work covers, and already have their own week-long non-immutable cache policy); the remaining 2,422 exactly equal the sum of every in-scope pattern's rewritten count (1,408 href/src-within-assets + 683 srcset + 190 og/twitter meta + 95 JSON-LD + 24 hero-rendered + 22 hero-marker). Plus 624 favicon/apple-touch-icon links (outside the `/assets/` prefix, counted and hashed separately). Total: 3,046 references hashed, zero missed, zero corrupted.
  **Verification gap closed in the same PR, per Lisa's instruction that this was the right moment for it:** no check previously validated that a `srcset`, `og:image`/`twitter:image`, or JSON-LD `"image"` path actually resolves to a real file — only check 2 covered href/src. New `audit.js` check 30 closes it, mirroring check 2's approach exactly (strip `?v=...` and `#fragment`, resolve an absolute `https://lisacolliorealtor.com/...` URL to root-relative, check against the same on-disk file set). Proven both ways: a mutation test in a scratch copy (not committed) planted a nonexistent path in each of the three newly-covered patterns on a real page and confirmed check 30 caught all of them; the live tree produces zero findings. The og:image/twitter:image regex requires the closing quote immediately after the property name specifically so it does not also fire on `og:image:width`/`:height`/`:alt` — verified by construction, not assumed.
  **Netlify headers now `immutable`, `max-age=31536000`, for `/assets/css/*`, `/assets/images/*`, `/favicon*`, and `/apple-touch-icon.png`** — safe now that the query string is part of the browser's cache key, so a changed file is a genuinely different URL and there is no window where a stale asset can still read as current. `/assets/fonts/*` is unchanged (`must-revalidate`, one week) since fonts remain deliberately unhashed. The netlify.toml comment explaining the old short-term fix is preserved as historical record, not deleted, with the new mechanism documented directly above it.
  **`package.json` "build" and "check" now include the hashing step** (`node minify-css.js && node build.js && node hash-assets.js`; `check` adds `node hash-assets.js --check`), so it is not a step a future session can forget to run.
- [x] **Master Plan lived only outside the repo until PR #92 — CLOSED 30 July.** Through v2.11, the governing Master Plan existed solely in the Claude Project's knowledge base and chat uploads; no version had ever been committed to this repo, even though CLAUDE.md, PR descriptions, and this punch list all cite it by version number. Committed for the first time as `docs/approved-copy/V2.13_Website_Master_Plan_Lisa_Collio.md` in PR #92; superseded by v2.14 in PR #93 (v2.13 deleted outright, not left as a stale prior version alongside it).
  **Why this matters more than the content-hashed-filenames item above:** this is the *second* time a document this project depends on existed only outside git. The first was this very punch list, which was itself a chat upload until 29 July (see the note at the top of this file). A governing document living outside the repo can silently diverge from what a session actually builds against — no diff, no version history, no way for a future session to notice drift before it's already shipped. The Master Plan's own §13 now states that regenerating CLAUDE.md is part of the same update whenever the plan version-bumps — but that rule is only checkable at all because the plan itself is now a file a diff can be run against.

## H. Broker review — the biggest remaining gate

Per Lisa's standing workflow the broker reviews the **entire finished site in one pass**, not page by page. Until then, all files stamp "Approved by Lisa" only.

**Exception, confirmed by Lisa (31 July 2026) — not a defect, don't re-flag it.** Five early-build documents in `docs/approved-copy/` legitimately carry individual "Managing Broker sign-off: RECEIVED" stamps with dates: `V1.0_ES_Viviendo_en_Goshen_Page_Lisa_Collio.md`, `V1.0_ES_Mudarse_a_Goshen_Page_Lisa_Collio.md`, `V1.0_ES_Articulos_Goshen_1-6_Lisa_Collio.md`, and `V1.0_ES_Legal_Pages_Lisa_Collio.md` (all 24 July 2026), plus `V1.0_ES_Disclaimers_Page_Lisa_Collio.md` (25 July 2026). The broker reviewed and signed off on those individually at the start of the build, then later decided to switch to the single comprehensive end-of-project review described above for everything built since. Both are real; they aren't in tension. A Part C2 repo audit (30 July 2026) initially misread these five as false claims — corrected here so a future audit doesn't re-raise the same false positive.

- [ ] Broker reviews the complete site
- [ ] Resolve whatever he flags
- [ ] Formal sign-off recorded

## I. Pre-launch technical & legal

- [ ] Set Privacy Policy and Terms effective dates
- [ ] End-to-end test all four forms, both languages
- [ ] Full Site Audit (Part B of the current Audit Checklist, `docs/approved-copy/`) against the final preview

## J. DNS cutover and immediately after

- [ ] Enable Netlify Forms email notifications to lisacolliorealtor@gmail.com
- [ ] Enable Netlify server-side Analytics — **not** Google Analytics, which would break the approved Privacy Policy and CSP
- [ ] Verify the domain in Google Search Console, submit `sitemap.xml`
- [ ] Update Google Business Profile website URL and NAP
- [ ] Actual DNS cutover
- [ ] Post-cutover spot-check: redirects, hreflang, and canonical URLs resolving on the real `lisacolliorealtor.com`, not just the Netlify subdomain

---

## Mechanical checks now in place

Recorded so future sessions know what is already enforced and don't rebuild it.

| Check | What it catches |
| --- | --- |
| `npm run check:images` | Every slug classified overlay-or-clean; in-scope files carry a band, out-of-scope files don't; every referenced file exists; section images recorded in `SECTION_JOBS` |
| `audit.js` check 14 | EN/ES image-count parity, using `content/hreflang-pairs.json`; baselined gaps can only shrink |
| `audit.js` check 15 | Fails the build if any page references one of the four rejected assets, in any variant |
| `audit.js` check 16 | FAQ image ↔ question-link agreement — catches an image attached to the wrong question |
| `audit.js` check 17 | Fair Housing banned terms in **alt attributes**, EN + ES, from `content/source/fair-housing-terms.txt`. Deliberately scoped to alt only — body copy is not gated |
| `audit.js` check 18 | Locked identity rules in **description fields** (meta, og, twitter, schema): REALTOR® without the mark, leading with the designation, wrong title form, `REMAX`, brokerage not written in full, "Northern Indiana", "Elkhart County" as a service area, missing comma, superseded phone. Carries an allowlist for the two correct "Elkhart County" proper names |
| `audit.js` check 19 | Locked identity rules in the **`<title>` tag** — the same rule set as check 18, applied to a field class check 18 never scanned. Caught 3 live "Realtor" violations on merge (PR #98, `audit.js:633`) |
| `audit.js` check 20 | Schema **`headline` ↔ on-page `<h1>`** must agree. Catches a title revised in one representation and not the other — the drift the Title/Identity-Text Consistency Sweep Standard exists for (PR #98, `audit.js:652`) |
| `audit.js` check 21 | **Sold-listing address claims** ("Sold by Lisa Collio" / "Vendida por Lisa Collio", `Sold: <address>`) must appear in `content/source/sold-listing-consent.txt`. 876 IAC 8-1-8(f) written-consent gate; backed by Lisa's confirmed records since 2 August (PR #98, `audit.js:676`) |
| `audit.js` check 22 | **Section-image batch reconciliation** — source files minus rejections == `SECTION_JOBS` entries == names built in `assets/images/sections/`, differenced set against set per source folder, never a running total (`audit.js:716`) |
| `audit.js` check 23 | **Same photo, same language, same alt text** — one source photo used in two places must not carry two different descriptions within a language. Built after an approved alt string was found attached to the wrong photograph (`audit.js:776`) |
| `audit.js` check 24 | **Governing-documents self-reference — §19's whole "Exact filename" column, widened 11 August 2026 (was row 1 only).** Fails if ANY row's Exact filename cell in the 4-row "Governing documents" table contains a version string, matched by `\bV\d[_.]\d` (generalized from `V2_[0-9]`/`v2\.[0-9]`, which was hardcoded to "2" and would never have caught the Audit Checklist's `V1_4`). Scoped to that one table on purpose — §19's other tables carry 24 legitimate, permanent version strings that must never be flagged, and the file carries dozens of correct dated "(NEW in vX.Y)" provenance tags outside any table. **Proven against a real known-bad case, not a planted one:** v2.19-on-`main` produces exactly 2 errors (rows 2 and 3, by name); the other §19 tables produce 0 despite their 24 real version strings; v2.20 produces 0. **Still does not cover the rest of the self-reference family** — two of the six known instances (v2.14, v2.17) were changelog undercounts, which no version-string check can catch, and two (v2.8, v2.11) were stale citations elsewhere in §19 outside this column. The count-the-body-against-the-changelog step stays mandatory before every bump (`audit.js:847`) |
| `audit.js` check 25 | **Screen-reader-only text matches its page's declared language** — `aria-label`, `alt`, the `title` attribute (not the `<title>` tag), and `.sr-only`/`.visually-hidden` text, across every EN/ES page. Exempts exactly two strings by exact match, the locked Master Plan §7 bilingual-toggle badges — nothing else. Built after `build-reviews.js`'s `stars()` helper was found hard-coding `aria-label="5 de 5 estrellas"` with no `lang` parameter, so every English page's star rating read aloud in Spanish (148 instances, 74 pages) — invisible to every prior check because nothing renders an aria-label visibly. **Proven four ways before trusting it:** true positive against the pre-fix tree (148/148, exact match to the measured defect); true negative post-fix (0, exit 0); the two exempt toggle strings produce zero false hits despite being deliberately other-language; a planted violation (a Spanish `alt` on an English page) was caught, then reverted clean. **Documented limitation, in the check's own comment:** the language guesser is deliberately conservative (diacritics + stopword-majority; ambiguous strings are skipped, not guessed) — it undercounts rather than over-flags, so a clean run is evidence for what it can judge, not a certificate that every screen-reader string sitewide is correct (`audit.js:890`) |
| `audit.js` check 26 | **Hero placeholder detection — ERROR (flipped from WARNING 13 August 2026, once all twelve real photos landed).** Two independent signals per hero section: the `_placeholder.jpg/.webp` filename and the `data-hero-placeholder="true"` attribute. Either without the other is also flagged, as a signal mismatch — catches a rename that isn't matched by the attribute, or vice versa. **Proven both ways at WARNING-level (12 August)** and **re-proven after the flip (13 August):** a mutation test reverting one live page to the placeholder correctly failed the run (exit 1, under ERRORS), restored clean (exit 0) |
| `audit.js` check 27 | **Fair Housing banned terms in hero copy** — `.hero__promise` (headline) and `.hero__identity` (H1/supporting line), reusing `content/source/fair-housing-terms.txt` rather than a second list that could drift from check 17's. Closes the "Proposed, not built" line below from the 5 August table. **Proven both ways:** zero false positives against the actual approved Tier 2 + Home hero copy (prototyped first, per Lisa's ruling, before anything else); a planted "quiet" in a throwaway copy of one page was caught and correctly attributed to `hero__promise`, then reverted clean |
| `audit.js` check 28 | **NOT a working WCAG contrast check — provides only two infrastructure catches (broken hero image reference; missing `scripts/hero-contrast-sample.py` or its Pillow dependency). The contrast-threshold branch cannot currently fail for any photograph and should not be read as coverage against a bad photo.** Samples the left 40% of each unique `--hero-img` file, blends it with the `.hero--photo` scrim at the CSS's real 82% opacity, and compares against `#fff`. **Checked the actual worst case, not just this check's own sampling choice:** even at the *lightest* opacity the scrim ever reaches while text can still be present — 66%, the gradient's 52% boundary, the edge of `.hero--tier2 .wrap`'s `max-width:780px` text column — a synthetic pure-white photo still blends to 6.68:1, comfortably over the 4.5:1 floor. So this isn't an artifact of a conservative sampling zone: **as long as the scrim opacity and the text column's width stay where they are today, no photo brightness can ever trip the threshold branch — it is structurally guaranteed by the CSS, not by this check.** The arithmetic and error-emission code path were confirmed correct separately, with the comparison threshold temporarily lowered in a scratch copy that was never committed. **Open decision for Lisa, not resolved here (see §C):** leave check 28 as the two infrastructure catches under an honest name, redesign it to instead guard the CSS invariant itself (flag if the scrim opacity or the text column's width ever changes, since either is the one way this could someday matter), or remove the contrast-threshold branch entirely as dead code. |
| `audit.js` check 29 | **Mobile nav open panel has a bounded, scrollable escape hatch.** Static, text-based (like every check in this file — it does not render a page). Verifies `assets/css/site.css` sets a bounded `max-height`/`height` together with `overflow(-y): auto`/`scroll`, either on a `:has(.nav-toggle__box:checked)` ancestor rule or on the toggle-revealed panel itself. Built after the mobile menu was found unreachable below the fold for ~2.5 weeks (see §C). **Sufficient only because the shipped fix is self-adjusting** — it could not catch a fixed-pixel `calc()` approach failing at some future breakpoint or label length; that needs a rendered-viewport measurement, tracked as a separate, deliberately-unbuilt gap in §C. **Proven three ways:** true positive against the real pre-fix `site.css` from `origin/main` (the actual file that shipped the bug, not a planted example); true negative against the fixed file; a mutation test flipping `overflow-y: auto` to `overflow-y: visible`. The mutation test itself surfaced an honest nuance: the fix ships two independent, redundant escape hatches (the `:has()` rule and its `@supports` fallback), so breaking only one still passes correctly — reachability is genuinely intact via the other. Only breaking both reproduces real unreachability, and that's what the committed mutation test does |
| `scripts/fair-housing-proximity-probe.js` | Body-copy banned terms, flagged only within 4 words of a place word (neighborhood/area/street/residential/community). 94% suppression, zero false positives and zero false negatives on the known set. **Committed but wired into no build step** — run manually as content changes |
| Proposed, not built | Widening the Fair Housing check from alt text to body copy using the proximity rule above (Lisa has the measurement; decision open) · phone-number convention in descriptions, once the ~130-description backlog in §E is cleared |

**Every check above was verified failing as well as passing** — deliberately broken with a planted violation to confirm it catches one — rather than only observed green.

> **Table backfilled 5 August 2026 — five enforced checks were missing from it.** Checks 19, 20 and 21 shipped in PR #98 and were described in no punch list at all (their only record was the §B entry stranded on an unmerged branch). Checks 22 and 23 shipped 2 August and were described in the body of this file but never added here. All five had been running on every build the whole time. Lisa's instruction covered 19–21; 22 and 23 were added in the same pass because leaving them out would have recreated the identical gap the same week it was closed. **This table's stated purpose is telling a future session what is already enforced — a check that is enforced but absent from it is worse than one that was never built, because the table reads as complete.**

**Build-record additions (why they matter):** `SECTION_JOBS` records source photo, focal point, crop, and the original's git blob SHA for every section image. `OVERLAY_JOBS` does the same for Template A featured images — added 29 July 2026 after a regenerated image had to be reverse-engineered by hash-matching, because nothing recorded which photo or colour variant built it. `generate_header_thumb()` brought the 800×420 card thumbnail into the script; it had been produced out of band, which is precisely why it couldn't be reproduced. **Every generated artifact needs a build record, or it becomes unreproducible the moment it needs changing.**

**`OVERLAY_JOBS` backfilled 30 July 2026 — 36 of 37 slugs recovered, 37/37 confirmed.** Prototyped on 5 slugs first (one with a known answer, to calibrate against), reported the honest result before running the rest — the prototype needed three real debugging rounds (a broken pixel-based band-height detector, an exact-hash criterion broken by font-rendering variance between environments, and a colour-detection bug that silently defaulted to blue on ties) before it reliably reproduced the one known-correct answer. Calibrated against that ground truth (photo-region pixel difference: 0.00 for a correct match, 40+ for wrong), then run across the remaining 36 at ~7s/slug. All 37 rows confirmed by regenerating and diffing the photo region only, which is immune to the font-rendering noise a full-image or byte-hash comparison would flag as a false failure.
**4 of the 36 initially came back "no confirmed match"**, not because the search failed but because their H1 has been revised since the image was built — the search was looking for the CURRENT title baked into artwork that carries an OLDER one. Confirmed visually on all four:
  | slug | baked into the image | current live H1 |
  | --- | --- | --- |
  | `costos-de-cierre-que-son` | ¿Qué son los costos de cierre? | Costos de cierre: qué son y qué incluyen |
  | `no-se-si-califico-credito-y-preaprobacion` | No sé si califico. ¿Por dónde empiezo? | "No sé si califico": crédito y preaprobación explicados |
  | `prestamos-fha-y-usda-en-espanol` | ¿Qué son los préstamos FHA y USDA? | Préstamos FHA y USDA, explicados en español |
  | `cuanto-vale-mi-casa-elkhart` | ¿Cuánto vale mi casa en Elkhart? | ¿Cuánto vale mi casa en Elkhart, Indiana? |
  Recorded in `OVERLAY_JOBS` against their baked title (so the row stays reproducible) with an inline comment flagging the drift. **Open decision for Lisa:** regenerate these four to match their current H1s, or leave the artwork as originally approved. Either way it's now a two-minute job instead of an archaeology problem.

## Standing workflow reminders

- Claude Code commits WIP to a feature branch — never `main`, never opening a PR — to survive container reclaim. That is preservation, not publication; the merge gate is unaffected.
- Every instructions file directs Claude Code to commit approved source documents into the repo as its first step, so Lisa never uploads to GitHub manually.
- Section 13: Claude drafts → Lisa approves → merge. Broker sign-off is once, comprehensively, at the end.
- Delete a branch when its PR merges. Leaving one alive is what stranded seven Spanish articles in PR #67.
- **A rule that exists only in a document is not enforced.** Three whole field classes reached production unchecked in one week — alt text, body copy, and description fields — each caught by eye, not by a gate. When a rule is settled, ask whether it can be made mechanical, and scope the check narrowly enough that it does not cry wolf.
