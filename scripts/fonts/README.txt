The .ttf files in this directory were converted from the site's self-hosted
assets/fonts/bricolage-grotesque-latin.woff2 (and -ext) using fontTools
(TTFont(woff2); flavor=None; save(ttf)) — the exact same font shipped on the
site. Bricolage Grotesque is licensed under the SIL Open Font License 1.1;
the full license text is in OFL.txt. Used here only for rendering blog
featured-image overlays offline (no third-party CDN).
